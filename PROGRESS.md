# FBR Admin — Estado do Projecto

> Lido no início de cada sessão; actualizado em tempo real durante a sessão.
> **Regras deste ficheiro:** máximo ~30 KB. Só as últimas 5 sessões ficam aqui, em formato compacto
> (template: O quê / Ficheiros / Migrações + passos manuais / Smoke / Pendente, máx ~15 linhas).
> Ao entrar a 6ª sessão, a mais antiga move-se **na íntegra** para o [PROGRESS-ARQUIVO.md](PROGRESS-ARQUIVO.md)
> (que NÃO é lido por defeito — todo o histórico das sessões 1-132 está lá). O detalhe fino vive nos commits do git.
> ⚠️ Hashes de commits do fbr-admin anteriores a 11/07/2026 foram reescritos no expurgo RGPD (sessão 139) — já não existem.

---

## Onde estamos

**Fase 6 — Integrações + PWA + RGPD (em curso).** Sessões recentes: **162** (2026-09-06, assistente: template base preenchida + saudação por hora + elogios com medida + pesquisa por telemóvel com "Colar número", **EM PRODUÇÃO** 06/09 via push do `d88671f`, sem migração), **160** (2026-09-06, resumo da encomenda ao vivo nos formulários do site + orçamento gravado ao entrar + Termos 2.0 + vale/rascunho/exemplos, **EM PRODUÇÃO** fbr-website `main` `2e0947d`, aprovado por ela na preview, sem migração), **159** (2026-09-02, assistente de resposta sem conversa + alertas do Dashboard só de "Entrega agendada" para a frente, **EM PRODUÇÃO** `3c70d10`, sem migração, smoke por fazer), **158** (2026-08-27, healthcheck a vermelho: mismatches de hidratação corrigidos, sem migração, por committar), **157** (2026-08-26 e 06-09, vidro museu passa a opção paga nos 2 repos + preços do site ligados à BD; migs 104 e 105 corridas), **156** (2026-08-21, templates listam os extras + mensagem pública editável no workbench, mig 103 corrida) e **155** (2026-08-19 a 21, detalhes da recolha no formulário público do site — **única sessão recente já fechada e confirmada pela Maria**, smoke feito). Antes: **154** (evento no Calendar para as secas, por committar), **153** (assistente do WhatsApp reconstruído) e **152** (converter encomenda para secas), todas **em produção mas sem smoke feito**. A 152 e a 153 correram em paralelo no mesmo dia — daí a colisão de numeração.

### ⚠️ Pendentes de confirmação da Maria (verificar antes de assumir)
- [ ] **Sessão 162 — Assistente (template base, saudação, elogios) + pesquisa por telemóvel (✅ EM PRODUÇÃO 06/09, `d88671f`, sem migração):** o diagnóstico e a implementação foram aprovados por ela na própria sessão ("por mim tudo ok"; push pedido por ela: "faz tu tudo"); falta o **smoke** no telemóvel (bloco da 162 em baixo, alíneas a-f). Fica por decidir depois: textos canónicos dos extras a partir das mensagens dela e emails no corpo de exemplos de voz.
- [x] **Sessão 160 — Resumo da encomenda + orçamento ao entrar + Termos 2.0 + vale/rascunho/exemplos (✅ EM PRODUÇÃO 06/09, fbr-website `main` `2e0947d`, merge --no-ff de `develop`; admin só teste+docs):** a Maria testou na preview e disse **"parece-me perfeito, go"**. Termos 2.0 decididos por ela (8 decisões). Preços do fundo fotografia confirmados como estando certos em Finanças. **Em aberto:** Livro de Reclamações (confirmar com a contabilista e registar; depois acrescento a frase aos Termos); smoke em produção de uma submissão real (o Turnstile não funciona na preview). **Smoke:** ver bloco da 160 em baixo.
- [x] **Mig 106 — templates do vale + factos do Cérebro (sessão 160, ✅ CORRIDA pela Maria 06/09):** [`106_templates_vale_restante_credito.sql`](supabase/migrations/106_templates_vale_restante_credito.sql) cria `vale_reserva_restante_pt/en`, acrescenta `{credito_vale}` aos `vale_reserva_coberta_*` (só se a frase-âncora não foi editada; senão pôr à mão) e junta o bloco "Actualização de Setembro de 2026" aos factos do Cérebro. Independente do deploy (variáveis vazias até lá). Queries de verificação no fim do ficheiro. **Correu à 2.ª:** a 1.ª tentativa deu `42P10` porque o índice único de `slug` é **parcial** (`WHERE deleted_at IS NULL`, mig 041) e o `ON CONFLICT` tem de repetir o predicado. Corrigido no ficheiro. **Verificada ✅** (query única UNION ALL, 06/09): 4 templates do vale, `{credito_vale}` nos 2 "reserva coberta", factos no Cérebro. ⚠️ O SQL Editor do Supabase só mostra o resultado da **última** instrução — para verificar vários SELECTs de uma vez, juntá-los com `UNION ALL`.
- [x] **Sessão 159 — Assistente sem conversa + alertas sem pré-reservas (EM PRODUÇÃO 02/09, `3c70d10` + `b60e482`, sem migração):** a Maria viu e disse **"parece-me bem / tudo ok"** no próprio dia — confirmação por alto, o smoke detalhado (a) a (h) não chegou a ser feito. o `SuggestComposer` passou a componente partilhado e funciona a partir da **encomenda** quando não há conversa nenhuma, incluindo na aba **Email** (formato de email, com assunto); os alertas do Dashboard passam a começar em "Entrega agendada" (cai o alerta "Pré-reserva sem contacto"). **Smoke:** ver bloco da 159 em baixo.
- [ ] **Sessão 158 — Healthcheck a vermelho resolvido (✅ EM PRODUÇÃO, `8756333` + `3a14f8a`, sem migração):** os 10 erros eram **8 mismatches de hidratação** (datas/horas calculadas em UTC no servidor e em Lisboa no browser) + **2 de ruído** do supabase-js. Corrigidos à raiz, não silenciados. **Só param de entrar depois do deploy**, e o cartão só volta a verde **24h** depois (a janela do check é de 24h) — ou logo, se ela apagar os registos antigos: `DELETE FROM client_errors WHERE at < now();`. **Smoke:** ver bloco da 158 em baixo.
- [ ] **Sessão 157 — Vidro museu passa a opção paga + preços do site ligados à BD (2 repos, ✅ EM PRODUÇÃO 06/09):** admin `5df948a` em `master`, site `6700394` em `main` (via `develop e685ceb`). **Mig 104** ([`104_museum_glass.sql`](supabase/migrations/104_museum_glass.sql)) ✅ **CORRIDA** (Maria, 26/08). **Mig 105** ([`105_museum_glass_costs.sql`](supabase/migrations/105_museum_glass_costs.sql)) ✅ **CORRIDA** (Maria, 06/09). **Nada pendente na BD.** **Copy do site ✅ FEITO** (PT+EN). **Só falta o smoke de interacção:** ver bloco da 157 em baixo.
- [ ] **Sessão 156 — Templates passam a listar os extras + mensagem pública editável no workbench (✅ EM PRODUÇÃO, `ae17b81`):** **Mig 103** ([`103_templates_resumo_encomenda.sql`](supabase/migrations/103_templates_resumo_encomenda.sql)) ✅ **CORRIDA** (Maria, 21/08). **Smoke:** ver bloco da 156 em baixo.
- [ ] **Sessão 154 — Flores secas geram evento no Calendar (✅ EM PRODUÇÃO, `a3edc2c`, sem migração):** as secas não têm data do evento, por isso o Calendar nunca disparava. Passa a valer a **data de entrega das flores**. **Smoke:** ver bloco da sessão 154 em baixo.
- [ ] **Sessão 153 — Assistente do WhatsApp reconstruído (EM PRODUÇÃO 17/08, 8 deploys, último `6f077f4`):** ponto de partida da Maria: *"o sistema de templates dá-me imenso trabalho e não me relaciono com o que o assistente diz"*. **Mig 102** ([`102_suggestion_edits.sql`](supabase/migrations/102_suggestion_edits.sql)) ✅ **CORRIDA** (Maria, 17/08). Nada disto está confirmado por ela ainda.
  - **Diagnóstico:** (a) o prompt nunca teve **uma única mensagem real dela** — só a persona escrita à mão e os 29 templates, por isso soava a template; (b) a query do `suggest` omitia ornamentos/pendentes/quadros extra/extras/tipo de flores, logo respondia às cegas a quem marcou "Mais info"; (c) as edições dela eram deitadas fora, o sistema nunca aprendia.
  - **7 frentes, todas live:** (1) o assistente **lê os campos do formulário** + secção **OBRIGATÓRIO** com o que a mensagem tem de cobrir (`requiredContentPoints`); (2) **voz aprendida** — 8 mensagens reais dela (`sent_echo`) anonimizadas e escolhidas pela situação entram no prompt, auto-alimenta-se; (3) **UX telemóvel** — fim do toast "Copiado" preso, botão "Abrir no WhatsApp" (wa.me com o texto), toques a 44px; (4) **rascunhos persistentes** em localStorage (sobrevivem ao Android matar a PWA) + **histórico `‹2/3›`**; (5) **afinação por texto livre** ("mais curta") que reescreve em vez de refazer; (6) **ciclo de aprendizagem** — ao copiar guarda o par gerado/usado; no Cérebro do Claude ela pede a análise e aceita/rejeita regras que entram no prompt; (7) sugestão **encolhível** para ler a conversa por trás.
  - **Decisões dela (não voltar a propor o contrário):** explicações dos extras **sem preços**; fundo do quadro não gera pendência; **sem atalhos de um toque** na afinação, só texto livre.
  - **🔴 SMOKE — nada confirmado:** (a) conversa com ornamentos/pendentes em "Mais info" ou envio "Não sei" → sugestão explica sozinha e sem preços; (b) o tom soa-lhe a ela e **nenhum nome de outra cliente** aparece; (c) copiar → "✓ Copiado" sem tapar botões; (d) "Abrir no WhatsApp" cai na conversa certa com o texto; (e) escrever meia mensagem → ir ao WhatsApp → voltar → texto lá; (f) Refazer 2-3× → `‹2/3›` e dá para recuar; (g) "mais curta" → Aplicar → encurta mantendo valores/datas; (h) chevron encolhe e vê-se a conversa; (i) usar 5+ vezes → Cérebro do Claude → "N por analisar" → Analisar → aceitar regra → Guardar → respeitada na sugestão seguinte.
  - **Por decidir (não aprovado):** textos canónicos dos extras (hoje o Claude improvisa); guia de voz destilado do corpus todo; rascunho pré-gerado ao abrir a conversa (U5); botão de microfone. Custos API: Haiku ~2,50€/mês · Sonnet ~9€/mês · Opus 5 ~19,50€/mês (10 sugestões/dia); modelo mantém-se `claude-sonnet-4-6`.
  - **⚠️ Efeito colateral no desktop:** a caixa da sugestão perdeu a pega de redimensionar (`resize-none`) porque cresce sozinha. Reverte-se se ela quiser.
  - **Correcção pós-deploy (21/08, `f45ea22`, EM PRODUÇÃO):** a Maria carregou em **"Refazer" e saiu exactamente o mesmo texto**. Causa: o prompt é **byte a byte idêntico** entre as duas chamadas, e a tarefa é constrangida demais (templates + pontos obrigatórios + exemplos de voz + factos fixos) para a aleatoriedade do modelo dar a volta sozinha — **mexer na `temperature` não resolveria, já está no default 1.0**. Correcção: o cliente manda as **2 últimas versões** em `avoid` e o prompt ganha um bloco **SEGUNDA TENTATIVA** que mostra o que já saiu e pede uma alternativa claramente diferente **na forma** (outra abertura, outra ordem, outras frases), mantendo factos/valores/datas/pontos obrigatórios. Só se aplica ao Refazer; a afinação por texto livre continua a reescrever a versão actual. **Smoke:** gerar → Refazer → estrutura diferente e `‹2/2›` no cabeçalho.
  - **Nota de método (sessões paralelas):** este commit foi feito com a sessão 156 a meio de trabalho no mesmo [suggest/route.ts](src/app/api/whatsapp/suggest/route.ts). Sem sobreposição de linhas; committado só o desta sessão construindo o blob "HEAD + as minhas alterações" e usando `git hash-object` + `git update-index --cacheinfo` (não mexe no working tree). ⚠️ **Armadilha:** a 1.ª tentativa reconstruiu o ficheiro por replace num heredoc e converteu `\n` em quebras de linha reais → literal de string multi-linha = **erro de sintaxe**. Apanhado antes do commit validando o blob com o parser do TypeScript. Se for preciso repetir isto, **partir do working tree e remover o que é da outra sessão**, nunca reaplicar as próprias alterações sobre o HEAD.
- [ ] **Sessão 152 — Converter encomenda para "flores secas" no workbench (EM PRODUÇÃO 17/08, `2dc4a7c`, sem migração):** select "Tipo de serviço" no rodapé passa a ter os 3 tipos, com diálogo de confirmação (preços passam a `secas_*`, orçamento recalcula só se ainda for o automático, "Flores na prensa" sai da timeline). Selo SECAS em **castanho da marca** (verde batia com a pílula "Contactada"); cartão do workbench verde. **Smoke:** abrir a encomenda da noiva → mudar para secas → orçamento cai, tag castanha, cartão verde, dropdown sem "Flores na prensa"; site de acompanhamento sem esse passo.
- [ ] **Sessão 151 — "No desidratador" + "na prensa há X" (migs 099/100/101 ✅ todas CORRIDAS):** selo/botão só na janela de ~1 mês após entrar na prensa; contador desde `in_dehydrator_at` (não desde a prensa). Botão de inverter ordem na vista Cards, persistido. **Smoke:** pôr um card no desidratador → selo laranja + "No desidratador há X"; tirar → some; encomenda >1 mês na prensa sem flag → sem botão; Concluídos/Cancelamentos colapsados.
- [ ] **Sessão 149 — Serviço "Recriação" (EM PRODUÇÃO 10/08, mig 098 ✅ CORRIDA):** carimbo interno, mesmos preços e acompanhamento público, nada no fbr-tracking. **Smoke:** abrir encomenda → tag "Recriação" no cabeçalho + rodapé "Tipo de serviço" → mudar → badge violeta na lista + aba no filtro.
- [ ] **Status das secas esconde "Flores na prensa" (2 repos, EM PRODUÇÃO 10/08, mig 097 ✅ verificada):** timeline pública renumera sozinha; selector de estado do admin esconde a fase. **Smoke:** (site) status de encomenda secas → sem "Flores na prensa", "Etapa X de 11" sem saltos; preservação → 12 passos. (admin) dropdown de estado das secas sem essa opção.
- [ ] **Sessão 148 — Templates "indeciso" + bug do desconto (mig 096 ✅ CORRIDA):** o orçamento editado à mão passa a mandar sobre o `pricing_snapshot` em todos os `{valor_*}`. Já em produção com os deploys da 153. **Smoke:** encomenda com desconto manual → template de pré-reserva mostra sinal proporcional ao orçamento editado.
- [ ] **Sessão 145 — Emoldurar Flores Secas (EM PRODUÇÃO 26/07, mig 094 ✅ CORRIDA + bucket `bouquet-photos` criado):** **Smoke (site):** `/reservar-emoldurar-flores-secas` (e `/en/book-dried-flower-framing`) → submeter pedido de teste com fotos → entra no admin com badge "Secas" e secção verde. **Smoke (admin):** marcar 1º pagamento com Google ligado → pasta Drive criada e fotos movidas do Storage. **Adiado:** criar encomenda secas à mão no admin; actualizar **Ecossistema** + `docs/ECOSYSTEM.md` com o form novo, o bucket e as rotas/tabelas da 153 ([[feedback_novas_plataformas_ecossistema]]).
- [ ] **Smokes antigos ainda por fazer (detalhe nos commits):** **144** fbr-voucher no telemóvel (envelope centrado, sem zoom preso); **143** fbr-website forms (telefone formata sozinho e valida por indicativo, sugestão de typo no email, `?vale=` pré-preenche, ecrã de sucesso repete o contacto); **141** fbr-website (FAQ do bouquet, link no CTA de Opções) + **GSC: pedir reindexação** das 6 páginas; **140** cards colapsados, diálogo de tracking CTT, bloco Comissões do parceiro; **138** fbr-voucher (envelope personalizado, pétalas, copiar código, partilhar); **135** título "FBR Admin" centrado no telemóvel; **133** login dos 3 perfis + aba WhatsApp + Ana sem editar Ideias/Livro de Receitas; **131** etiquetas do WhatsApp; **130** sino das notificações na PWA.
- [x] **Google Maps — ✅ RESOLVIDO na 155 (19/08):** a Maria pôs cartão no Google Cloud. Ficam **3 chaves ao todo**: a antiga "Maps Platform API Key" (browser, referrer, 4 APIs) que serve o **admin**, mais duas novas no **fbr-website** (`GOOGLE_MAPS_KEY` servidor + `NEXT_PUBLIC_GOOGLE_MAPS_KEY` browser). Custo real 0€. Ela criou a chave de servidor no projecto **fbr-admin2 por engano** à primeira (o selector de projecto da Vercel fica no último aberto) e já a **apagou de lá ✅**. Nada pendente.
- [ ] **Backup pré-expurgo (sessão 139):** `_privado/backup-pre-expurgo/fbr-admin-completo-2026-07-11.bundle` CONTÉM histórico antigo com PII — apagar depois de umas semanas de confiança.

### Próximo passo concreto
1. **A Maria usar o assistente do WhatsApp uns dias e fazer os smokes da 153.** É o passo mais valioso e não é código: nunca se mediu quantas mensagens `sent_echo` existem na BD, e é esse número que decide se a peça da voz funciona. O ciclo de aprendizagem também precisa de 5+ pares para ter o que analisar.
2. Depois disso, por ordem: **textos canónicos dos extras** (barato, sem migração, tira improvisação; **na 162 a Maria confirmou que quer as mensagens antigas dela como base**, ver "fica por fazer" da 162) → **guia de voz destilado do corpus todo** → **U5, rascunho pré-gerado** ao abrir a conversa (gasta API, deixar para quando a qualidade base estiver validada).
3. ~~Decidir a Google Maps~~ ✅ **RESOLVIDO na 155:** a Maria pôs cartão no Google Cloud, criou 2 chaves e o Maps está live no site e no admin. Custo real 0€ (dentro dos 10.000 pedidos grátis/mês de cada API).
4. Roadmap 124 que sobra: tipos gerados do Supabase no preflight (precisa do access token dela); vista "Hoje" no Dashboard + relatório mensal interno.

---

### Fases do projecto
- [x] **Fase 1** — Fundação: Supabase ligado, autenticação, layout/navegação ✅
- [x] **Fase 2** — Preservação de Flores: tabela, workbench, estados, orçamento, permissões ✅
- [x] **Fase 3** — Vale-Presente (admin + site público `voucher.floresabeirario.pt`) + Status ✅
- [x] **Fase 4** — Dashboard + Tarefas + Métricas ✅
- [x] **Fase 5** — Formulários públicos + Parcerias ✅
- [x] **Fase 5.5** — Afinações pós-uso ✅
- [~] **Fase 6** — Integrações + PWA + RGPD completo ← **EM CURSO**

---

## O que está feito (estado actual da plataforma)

- Next.js 16 + shadcn/ui + Supabase ligado, deploy em `admin.floresabeirario.pt`
- Login Netflix com fotos, **email+password** (António admin, MJ admin, Ana viewer); permissões admin/viewer em todas as abas; gate de equipa no proxy (sessão 124); policies centralizadas em `is_team_admin()`/`is_team_member()` (mig 085) + `TEAM` em [roles.ts](src/lib/auth/roles.ts) como fonte única no código
- **Preservação**: 4 vistas (Tabela / Cards / Calendário / Timeline), grupos colapsáveis, drag-and-drop, workbench 3 colunas (refactorizado na 128: orquestrador 436 linhas + 12 componentes em `_components/`), edição inline, alertas 40%/30%/aprovação, vistas/filtros/colunas guardáveis (sessão 95), detecção de clientes repetidos (avisa, nunca bloqueia), dark mode
- **Vale-Presente** admin + site público `voucher.floresabeirario.pt`
- **Status** admin + site público `status.floresabeirario.pt` (12 fases públicas PT/EN, data prevista auto +6m; redesign "Herbário" na 123)
- **Parcerias** completas (4 categorias, mapa Portugal, interações, acções, Nominatim) + Figuras Públicas
- **Dashboard** com afazeres globais em kanban GTD, recolhas/entregas, alertas; tarefas multi-assignee com lembretes data+hora
- **Métricas** + **Finanças** (6 sub-abas: Painel / P&L por encomenda / Catálogo / Despesas / Faturação / Competição; COGS tudo-ou-nada; helpers em [lib/finance.ts](src/lib/finance.ts))
- **Entregas e Recolhas** (agenda + mapa + notas) · **Livro de Receitas** · **Chat interno** (Realtime) · **Ideias** · **Healthchecks** (com monitorização de erros client-side, mig 086) · **Ecossistema**
- **Pesquisa global** Cmd+K em 5 tipos · **PWA** instalável (iOS + Android) com **notificações push internas** (sessão 130: na hora + diárias 7h + lembretes pontuais via GitHub Actions)
- **Integrações Google**: OAuth, pastas Drive auto ao 1º pagamento, Calendar, **Gmail no workbench** (só-leitura, sessão 105)
- **WhatsApp Cloud API** end-to-end (sessões 97-99): webhook, aba `/whatsapp` com avatares/vistos/etiquetas geríveis, media→Drive; registo manual por workbench também existe (sessão 65)
- **Assistente AI "Claude"** (Anthropic API, sessão 119 v2) + **Templates de mensagens** (29 PT+EN, picker com snippets/pesquisa, pares PT/EN na gestão)
- **RGPD**: exportação JSON+PDF, retenção 10 anos com anonimização, audit log UI
- **Backup diário da BD → Drive** (sessão 124: cron 05:00 UTC, 22 tabelas, rotação 14d + mensais + Janeiros; healthcheck próprio)
- **Forms públicos fechados de ponta a ponta** (mig 084 + service role no site; Turnstile server-side) · **CI** GitHub Actions corre `npm run preflight` · anti-drift tipos↔BD no preflight ([lib/schema-drift.ts](src/lib/schema-drift.ts))
- 92 migrações (006/014 são stubs — conteúdo com PII expurgado do histórico na sessão 139); 100 testes vitest; smoke Playwright (`npm run smoke`)

---

## Últimas sessões (detalhe compacto)

### Sessão 162 (2026-09-06) — Assistente: template base preenchida, saudação por hora, elogios com medida + pesquisa por telemóvel (EM PRODUÇÃO 06/09)
- **Ponto de partida da Maria (4 queixas):** (1) *"invisto tempo nas templates e o sugerir resposta reescreve-as"*; (2) *"cumprimenta 'boa tarde' em todas as mensagens, mesmo com o cliente a escrever há 2 minutos"*; (3) *"pouco profissional, lambe-botas, 'que ideia fantástica'"* → ela corrigiu: **é só uma inclinação, "nem 8 nem 80", afinar de leve, não proibir**; (4) explicação dos ornamentos/quadros pequenos saiu esquisita e teve de copiar as mensagens antigas dela. Diagnóstico aprovado por ela ("tudo o resto ok").
- **Diagnóstico:** as templates escolhidas por `fieldSuggestionBases` iam ao prompt **só pelo slug**, enterradas na biblioteca inteira, enquanto 8 exemplos de voz + persona + regras de voz + "integra de forma natural, não como lista" puxavam para reescrever. A conversa ia **sem horas nem "agora"** e a persona da BD diz literalmente "estrutura habitual: saudação com o primeiro nome + emoji". Não havia regra nenhuma sobre elogios. Os "Mais info" dos extras só dizem "explica o que são, sem preços": não há texto dela, logo improvisa (já estava em "por decidir" desde a 153).
- **1. Template base preenchida ✅.** [suggest/route.ts](src/app/api/whatsapp/suggest/route.ts): a rota busca a encomenda principal completa (+ valor do vale, como no workbench) e **renderiza** as templates da fase com `renderOrderTemplate` (na língua provável, fallback à outra), que vão num bloco **"TEMPLATE BASE — é isto que a mensagem tem de ser"** logo a seguir à encomenda: manter frases e ordem, só mudar o que a conversa obriga, encadear várias com uma só saudação, exemplos de voz só para o que for novo. O bloco OBRIGATÓRIO deixa de dizer "não como lista" e passa a "se a template já cobre, mantém a forma dela". A biblioteca no system passa a ser referência secundária. `settingsForPayment` subiu para antes do render. `{saudacao}` usa `lisbonWallClock` (o servidor está em UTC: às 19h30 saía "Boa tarde").
- **2. Transcript com horas + regra de saudação decidida em código ✅.** [transcript.ts](src/lib/whatsapp/transcript.ts) (novo): cada linha `[06/09/2026, 14:58 · há 2 min] CLIENTE: …`, linha "Agora são … (hora de Lisboa)" no topo, e bloco **"Saudação: SIM/NÃO"**: NÃO quando a última mensagem (de qualquer lado) tem < 3 h e é do mesmo dia em Lisboa. Só para WhatsApp; por email a saudação é sempre parte do formato. 11 testes ([transcript.test.ts](src/lib/__tests__/transcript.test.ts)).
- **3. Regras fixas coladas à persona ✅** (`REGRAS_FIXAS` na rota, não editáveis no Cérebro): prioridade da template base; **elogios com conta e medida** (não elogiar a FBR nem qualificar a escolha do cliente; "parabéns" quando a template o tem ou o cliente partilha algo pessoal, uma vez só); responder primeiro ao que foi perguntado.
- **4. Pesquisa global reconhece telemóveis + "Colar número" ✅** (pedido dela a seguir: *"faz com que a barra de pesquisa dê para pôr telemóveis"*). O WhatsApp Business no Android **não partilha para outras apps** (nem a conversa nem a mensagem: capturas dela), por isso o atalho WhatsApp → encomenda passa a ser: "Ver contacto" → tocar no número (copia) → abrir a PWA → pesquisa → **"Colar número do telemóvel"**. [phone-search.ts](src/lib/phone-search.ts) (novo): a pesquisa que pareça um número (só dígitos e separadores, 6+ dígitos) reduz-se a dígitos e casa pelo **fim do número** (últimos 9), seja como for que esteja guardado ("+351 910 843 885" ≡ "910843885"); o `ilike` cru nunca batia. [search/actions.ts](src/app/(admin)/search/actions.ts): nessa situação puxa os telefones de `orders`, `vouchers` e `whatsapp_conversations` (até 2000 linhas) e filtra em JS; acertos vão à frente, sem duplicados. [global-search.tsx](src/components/global-search.tsx): botão no estado vazio da paleta lê a área de transferência; **uma encomenda só → abre-a logo**, senão mostra a lista. Placeholder passou a "Nome, telemóvel, código, email, ID…". 7 testes. ⚠️ O compilador do React rejeitou `useCallback` à volta do `goTo` (regra `preserve-manual-memoization`) e o efeito não pode usar `goTo` antes da declaração: a decisão de abrir vive dentro do próprio handler do botão, não no efeito da pesquisa.
- **Preflight ✅** (tsc + 235 testes + build + eslint dos ficheiros tocados). **EM PRODUÇÃO 06/09** (`d88671f`, push pedido por ela). Sem migração.
- **Fica por fazer (diagnóstico aprovado, implementação não pedida ainda):** (a) **textos canónicos dos extras** a partir das mensagens `sent_echo` dela → 3 templates (ornamentos, quadros pequenos, pendentes) para ela aprovar; (b) emails enviados pelo info@ no corpo de exemplos de voz + botão "usar como exemplo" numa mensagem dela; (c) ~~WhatsApp → encomenda~~ resolvido pelo ponto 4 (colar número); a via Web Share Target morreu porque o WhatsApp Business não tem "Partilhar" para fora; (d) ponto "não tem o âmbito todo da encomenda" precisa de um caso concreto dela.
- **Smoke:** (a) conversa em curso (última mensagem há minutos) → sugestão **sem** "Boa tarde"/nome na abertura; (b) conversa retomada horas depois ou no dia seguinte → com saudação certa para a hora de Lisboa; (c) pré-reserva sem sinal → a sugestão é a template `pre_reserva_*` com valores certos, frases e ordem iguais (só acrescentos onde a conversa obriga); (d) vale-presente ou primeira mensagem → sem "que ideia fantástica"; (e) sugerir email → continua com assunto e saudação. (f) No telemóvel: WhatsApp → Ver contacto → tocar no número → PWA → pesquisa → "Colar número do telemóvel" → abre a encomenda directamente (o Chrome pede permissão à 1.ª vez); número com 2 encomendas → lista; escrever "910 843" à mão → encontra pelo pedaço.

### Sessão 161 (2026-09-06) — "Abrir no WhatsApp" nos templates (em curso)
- **Ponto de partida da Maria:** *"onde tens sugerir resposta, quero um botão a dizer abrir no WhatsApp e a mensagem aparece automaticamente escrita lá"* → esclarecido: o assistente já tinha o botão desde a 153; o que faltava era no **picker de Templates**.
- **1. Templates com "Abrir no WhatsApp" ✅.** [template-picker.tsx](src/components/template-picker.tsx): prop `phone` (na encomenda vem de `order.phone`; vale e lead passam o seu), e o diálogo de pré-visualização ganha um botão verde `wa.me?text=` que **respeita as edições** feitas em "Editar antes de copiar". Sem telemóvel, fica só o Copiar. Callers actualizados: [vale-presente workbench](src/app/(admin)/vale-presente/[code]/workbench-client.tsx) (`sender_phone`) e [suggest-composer](src/components/suggest-composer.tsx) (scope lead). `tsc` limpo.
- **2. O assistente passa a ler também os emails ✅.** O `suggest` só via `whatsapp_messages` e podia contradizer o que já tinha sido combinado por email. Agora puxa também as threads do Gmail da cliente (`fetchEmailHistory`, últimas 8 mensagens, corpos cortados a 700 caracteres e **sem citações**) e mete-as no prompt como "outro canal, vale como já dito". Vai em `Promise.all` com os exemplos de voz para não somar esperas; se o Google estiver desligado ou o Gmail falhar, segue só com o WhatsApp. `email` entrou nas colunas da encomenda no `LINKED_ORDER_COLUMNS`.
- **3. Selos de actividade nas abas Email/WhatsApp ✅.** [channel-activity.tsx](src/app/(admin)/preservacao/[id]/_components/channel-activity.tsx): ponto colorido + "há 3 d" em cada aba; **âmbar a piscar quando a última mensagem foi do cliente**, cinzento/azul/verde quando fomos nós. O lado do WhatsApp lê uma linha de `whatsapp_conversations`; o do email usa uma rota nova e leve, [/api/google/emails/summary](src/app/api/google/emails/summary/route.ts) (`fetchContactEmailSummary`: listar ids + ler cabeçalhos do mais recente, sem puxar corpos). **A aba por defeito continua a ser a preferência de contacto do cliente** (decisão dela).
- **4. Emails sem lençóis de citações ✅.** [email-quotes.ts](src/lib/email-quotes.ts) (`splitQuotedEmail`) corta na atribuição do Gmail (PT/EN, inclusive partida em 2 linhas), nos separadores do Outlook e no `>`. O painel mostra só o que foi escrito de novo, com "mostrar texto citado" a revelar o resto. Nunca deita nada fora: se o corte deixasse a mensagem vazia (resposta escrita por baixo da citação), devolve tudo. 6 testes.
- **5. Negrito no essencial, sem asteriscos a sobrar ✅.** Queixa dela: *"copio como bold e depois tenho que estar a apagar asteriscos"*. O assistente passa a escrever **sempre** no marcador canónico `**assim**` (2-3 destaques no máximo: valores, datas, prazos, códigos, morada; nunca a saudação nem o nome) e [rich-text.ts](src/lib/rich-text.ts) converte à saída conforme o destino: **WhatsApp** (copiar e `wa.me`) → `*assim*`, que é o negrito nativo; **email** (copiar) → `ClipboardItem` com `text/html` (`<b>`) + `text/plain` sem marcadores, portanto cola a negrito a sério no Gmail; **mailto:** → texto limpo. A rota normaliza a resposta do modelo (`normalizeBold`), por isso mesmo que ele escreva à WhatsApp o cliente só vê um formato. Templates copiados do picker passam pela mesma conversão. 8 testes.
- **1b. "Abrir no email" a par do "Abrir no WhatsApp" ✅** (pedido dela a seguir): mesmo diálogo do template, botão azul com o destinatário já preenchido. O `mailto:` passou a ser construído por um helper partilhado, [mailto.ts](src/lib/mailto.ts) (assunto separado do corpo, marcadores de negrito removidos, tecto de 1900 caracteres para o corpo não ir cortado em silêncio) — o assistente e o picker deixam de ter regras próprias. Templates são de WhatsApp e raramente trazem linha de assunto: quando não trazem, o assunto fica em branco para ela escrever (inventar seria pior). 5 testes.
- **Preflight ✅** (tsc + vitest + build, 217 testes). Commits `b3e8d4a` + `ff10f06` em `master`, ✅ **EM PRODUÇÃO** (push 06/09; a outra sessão em paralelo já tinha empurrado o `b3e8d4a`). **Smoke por fazer.**
- **Smoke:** (a) Inserir template → botão verde abre o WhatsApp na conversa certa com o texto, incluindo edições feitas em "Editar antes de copiar"; encomenda sem telemóvel → só Copiar. (b) Encomenda com emails trocados → aba Email com ponto e data; se o último email foi da cliente, ponto âmbar a piscar. (c) Email com 4-5 respostas → só a resposta nova, "mostrar texto citado" abre o resto. (d) Sugerir resposta no WhatsApp → copiar → colar no WhatsApp: negrito a sério, **zero asteriscos à vista**. (e) Sugerir email → copiar → colar no Gmail: negrito a sério, sem asteriscos. (f) Cliente com quem trocaste emails → a sugestão do WhatsApp já não repete o que foi dito por email.

### Sessão 160 (2026-09-06) — Resumo da encomenda ao vivo no formulário + orçamento gravado ao entrar + Termos 2.0 (rascunho)
- **Ponto de partida da Maria:** *"quero um resumo da encomenda com tudo o que o cliente escolheu e o preço de cada coisa, e as três fases de pagamento bem explícitas, para as pessoas não serem surpreendidas quando confirmo o orçamento"* + *"os clientes pensam que recebem o quadro numa semana"* + *"termos e condições têm de ser muito mais desenvolvidos"* + *"quem reserva para 2027 não pode passar à frente de quem tem o ramo esta semana"*.
- **1. Resumo da sua encomenda (fbr-website, 2 formulários).** Secção nova antes do botão, actualiza-se a cada escolha: quadro, fundo (só a fotografia custa; **cor não se cobra**, decisão dela), vidro museu, minis + vidro/foto dos minis, ornamentos, pendentes, elementos (incluído), envio/recepção (custos fora), **total estimado**, **3 fases 30/40/30 com cêntimos** e **previsão de entrega** ("por volta de dezembro de 2027" = evento + 6 meses; secas: 3 ou 6 meses conforme a abordagem). "Ainda não sei" no tamanho → "a partir de 300€" (mesma regra provisória do admin); "mais informações" nos extras → "a confirmar (35€ cada)", sem somar. Nota fixa: *"é uma estimativa, confirmamos em 3 dias úteis depois de lermos as notas"*. A caixa dos Termos passou para dentro desta secção. Componente partilhado [ResumoEncomenda.jsx](../fbr-website/fbr-website/app/_components/ResumoEncomenda.jsx); cálculo em `app/_lib/orcamento.js`, **espelho** de [pricing.ts](src/lib/pricing.ts).
- **2. Orçamento gravado ao entrar.** As encomendas do site chegavam sem orçamento (ficava vazio até ela carregar em "Calcular"). A API passa a gravar `budget` + `pricing_snapshot` (`orcamento-server.js`: lê `pricing_items` com a service role; sem acesso cai no mapa de preços da página). No admin continua "automático" enquanto `budget == snapshot.total` → recalcula sozinho ao decidir o tamanho; ela edita à mão (descontos) e passa a "Auto · editado". **Nada mudou no admin** além do teste.
- **3. Teste de paridade** em [website-pricing-contract.test.ts](src/lib/__tests__/website-pricing-contract.test.ts): corre o `orcamento.js` do site contra o `computePricingSnapshot` do admin com >1000 encomendas (3 serviços × 6 tamanhos × 5 fundos × 3 vidros × 3 vidros-mini × 4 flags × 3 qty). Falha se alguém mudar uma regra só num lado. O contrato de chaves passou a ler `precos-valores.js` (o mapa `PRICE_KEYS` mudou de ficheiro para poder ir para o browser).
- **4. "Cerca de 6 meses" em todo o site** (PT+EN: messages, FAQ, landing do bouquet, 5 artigos do blog). Ela não quer "até": é artesanal e imprevisível. **Sem checkbox** "sei que demora 6 meses" (rejeitou). O prazo entra pela previsão calculada no resumo + Termos.
- **5. Prioridade cronológica.** Copy nova em `reservar.passos[1]`, `formSub`, nota do resumo e ecrã de sucesso: *"se o evento for daqui a 6 meses ou mais, a resposta pode demorar mais do que 3 dias úteis: respondemos por ordem de proximidade da data"*. A frase condicional só aparece a quem tem evento a ≥6 meses (`eventoDistante`).
- **6. Termos e Condições 2.0 — RASCUNHO, não publicado.** Artefacto https://claude.ai/code/artifact/ffe9b884-79b6-45d2-a28f-c2f6bd78fc12 : 23 cláusulas (eram 13), com base em 6 atelieres (PT/UK/CA/US) + DL 24/2014, DL 84/2021, Lei 144/2015. **8 decisões da Maria pendentes** (prazo da 2.ª fase, cliente que não responde, guarda de peça pronta, flores não usadas, reagendamento, flores em mau estado, portefólio, entidade RAL). Só entra no site (PT+EN) com o OK dela; recomendada revisão jurídica das cláusulas 11/13/14/19.
- **6b. Termos 2.0 APLICADOS no site (2.ª ronda, mesma sessão).** A Maria aprovou o rascunho com 4 alterações: reagendamento **24 meses** (não 12); flores sem condições → **preservamos no estado em que chegam, flores de substituição a cargo do cliente**, sinal não devolvido mas transferível 24 meses (recomendação minha, **à espera da palavra dela**); fotos das peças são **obra e propriedade da FBR, sem direito de retirada** (só retratos/nomes precisam de autorização); **sem frase do Livro de Reclamações** (não tem; passa recibos verdes; pelo DL 74/2017 é provavelmente obrigatório mesmo para ENI, confirmar com a contabilista e registar em livroreclamacoes.pt). 23 cláusulas em `termos.sections` PT+EN; `consent_version` das encomendas novas passa a **"2.0"** (vales ficam "1.0-implicit"); (a `.legal-text` já tinha `white-space: pre-line`, os parágrafos com `
` funcionam). `develop` empurrado para o GitHub (preview da Vercel) porque ela foi testar e o commit ainda era só local.
- **Nota da preview:** o Turnstile dá "Não foi possível conectar" no domínio `*.vercel.app` porque esse domínio não está na lista de domínios do widget na Cloudflare → na preview **não dá para submeter**; testar a submissão em produção ou juntar `fbr-website-git-develop-floresabeirario.vercel.app` aos domínios do Turnstile.
- **1b. Redesenho do resumo (3.ª ronda, pedidos dela ao ver a preview):** (a) **barra fixa no fundo do telemóvel** com o total a mudar em tempo real (`BarraTotal` dentro do ResumoEncomenda; só ≤860px; aparece quando a secção "O quadro" entra no ecrã, esconde-se quando o resumo está visível, sobe para cima do banner de cookies medindo-o por `[data-cookie-banner]` + ResizeObserver; tocar faz scroll ao resumo); (b) **"Plano de pagamentos"** com a percentagem à frente (30% / 40% / 30%), título claro por fase ("Sinal para reservar a data") e subtítulo com o prazo (24h / 5 dias / antes da entrega); (c) previsão de entrega num só parágrafo; blocos uniformes (rótulo pequeno em maiúsculas + fio), sem caixa dentro de caixa. Smoke local a 390px ✅ (barra por cima do banner de cookies, escondida no desktop e com o resumo no ecrã). Dúvida dela em aberto: **se ver o preço a mudar desincentiva os extras** → medir depois no Umami (evento `reserva-seccao-resumo` já existe) comparando a % de reservas com extras antes/depois.
- **1c. Melhorias aos formulários (4.ª ronda, aprovadas por ela de uma lista de 10):** (1) **vale-presente no resumo**: `/api/verificar-vale` devolve valor/validade/expirado (nome e mensagem continuam a não sair), o resumo mostra "Vale-presente X: −300€" + "A pagar" e as **3 fases calculam-se sobre o que falta** (decisão minha, confirmar com ela; se o vale cobrir tudo, "A pagar 0€" e nota); o form das secas passou a verificar o código; (5) **"Ver exemplo"** com fotografia nos minis/ornamentos/pendentes (`ExemploModal`, imagens da página de Opções); (6) **rascunho no localStorage** (`use-rascunho.js`, 7 dias, sem Termos/honeypot; aviso destacado "o pedido ainda não foi enviado" + "Começar de novo"; apaga-se ao submeter, com guarda contra o temporizador dos 500ms); (7) **ecrã de sucesso** repete o resumo (sem barra) + "Próximos passos"; (8) evento **"Outro" → "Qual?"** (vai à cabeça de `additional_notes`: "Tipo de evento: X", sem coluna nova). **Rejeitados por ela (não voltar a propor):** cupão 5% no form (medo de desvalorizar), aviso de evento a dias/passado (responde rápido), foto do bouquet na preservação, autocompletar na localização do evento, mudar a ordem das secções. Smoke local com mocks (vale, submissão) ✅ sem erros.
- **1d. Vale-presente, regras fechadas pela Maria (06/09):** (a) **restante paga-se de uma só vez** na confirmação da reserva (sem limiar; "nunca aconteceu" um restante grande) → o resumo troca as 3 fases por uma linha "Restante a pagar, em 24h"; (b) vale maior que o pedido → **"Ainda tem X€ de crédito por usar"** + sugestão do que cabe (ornamento/pendente/mini com preços reais), ideia dela; (c) **Termos cláusula 15** actualizada PT+EN com as duas regras (site + artefacto). Substitui a decisão provisória "3 fases sobre o restante" da 1c.
- **1e. Barra do total também no PC** (pedido dela: o formulário é longo): acima de 860px passa a cartão flutuante centrado, 640px (largura da coluna do formulário), cantos redondos e sombra; mesma lógica de aparecer/esconder. Smoke desktop ✅.
- **1f. Templates do vale alinhados (aprovado por ela, textos lidos):** [templates.ts](src/lib/templates.ts) ganha `valeComRestante()` + variáveis `{codigo_vale}` (agora também na encomenda), `{valor_vale_desconto}`, `{valor_restante}`, `{credito_vale}` (frase pronta, vazia sem sobra); o picker sugere `vale_reserva_restante` quando orçamento > vale, senão `vale_reserva_coberta`. O valor do vale **não é coluna**: o [page.tsx](src/app/(admin)/preservacao/[id]/page.tsx) do workbench lê `vouchers.amount` e cola-o à encomenda como `gift_voucher_amount` (`OrderWithVoucher`); na Caixa de Entrada (sem esse valor) fica o comportamento antigo. **Sem tocar** em template-picker.tsx/suggest-composer.tsx (a sessão 161 está a mexer neles). Site: 2 condições novas na página Oferecer + parágrafo "quem decide" na cláusula 15 (EM PRODUÇÃO `423fc20`). **Rejeitado por ela:** prazo da 2.ª parcela no template; validade no template ao remetente; "prioridade" e "5 dias" nos factos. Preflight ✅ (40 testes em templates).
- **1g. Vale-presente → reserva (pergunta dela: "não estou a encontrar esse botão"):** a ligação só existia num sentido (encomenda → vale, no cartão de origem). A barra de "cliente repetido" da página do vale procura pelo contacto do **remetente**, e quem faz a reserva é quase sempre a **pessoa presenteada** com outro email/telemóvel, por isso nunca mostrava a reserva certa. A secção **Utilização** passa a ter "Reserva feita com este vale": match por `orders.gift_voucher_code`, em **lista** (o mesmo código pode aparecer em mais do que uma encomenda: engano, ou crédito que sobrou), cada linha com nome, estado e orçamento, a abrir a encomenda; sem reservas diz "ainda não foi usado numa reserva de preservação". Sem migração. Preflight ✅. **Smoke (Maria):** abrir um vale já usado → ver a encomenda em Utilização e clicar; abrir um vale novo → "ainda não foi usado".
- **Ficheiros (fbr-website, branch `develop`):** `_components/ResumoEncomenda.jsx` + `.css`, `_lib/orcamento.js`, `_lib/orcamento-server.js`, `_lib/form-enums.js` (NOVOS) · `_lib/precos-valores.js` (+PRICE_KEYS, +fotografia) · `_lib/precos.js` · `_lib/supabase-mappings.js` (mapas partilhados saem daqui) · os 2 forms · as 2 rotas da API · messages pt+en · faq-data(-en).jsx · preservar-bouquet-noiva/page.js · 5 mdx. **(fbr-admin):** o teste de contrato + este ficheiro + docs/ECOSYSTEM.md.
- **Migrações: nenhuma.** As colunas `budget`/`pricing_snapshot` já existem; a service role escreve-as.
- **Preflight:** site `next build` ✅; admin `npm run preflight` ✅ (ver commit). **Smoke local num browser real** (playwright-core no scratchpad + Chromium do ms-playwright, 390px): ver resultado no fecho da sessão.
- **Por confirmar com a Maria:** os preços do **fundo fotografia** que o resumo mostra vêm de Finanças → Catálogo (`fotografia_30x40/40x50/50x70`, sementes 15/25/35€); nunca consegui ler os valores reais em produção. Se estiverem errados, corrige-os lá e o site segue.
- **Decisão 6 dos Termos fechada pela Maria (06/09):** sinal **não se devolve, transfere-se** para uma encomenda futura até 24 meses. É o texto que já está no site.
- **Adiado por ela ("não me vou focar nisso agora"):** calculadora do custo da recolha → registada em "Próximas frentes". Triagem por proximidade do evento nas pré-reservas: sem decisão, só proposta.
- **🔴 Smoke (Maria, no telemóvel, preview da Vercel do `develop`):** (a) /reservar-preservacao → escolher 40×50 + fotografia + vidro museu sim + 2 minis com vidro + 1 ornamento → resumo mostra 7 linhas, total e 3 fases com cêntimos; (b) "Ainda não sei" no tamanho → "a partir de 300€"; (c) data do evento em 2027 → "por volta de {mês} de 2027" + frase da prioridade; (d) submeter um pedido de teste → no admin abre com o **orçamento já preenchido** e igual ao do site; (e) /en/book-preservation → tudo em inglês; (f) /reservar-emoldurar-flores-secas → preços das secas (200/270/360) e previsão 3 ou 6 meses conforme a abordagem; (g) FAQ e Como Funciona dizem "cerca de 6 meses".

### Sessão 159 (2026-09-02) — Assistente de resposta sem conversa + alertas do Dashboard só a partir de "Entrega agendada"
- **Ponto de partida da Maria (duas coisas):** (1) *"preciso do assistente mesmo quando o cliente nunca mandou mensagem — ele preenche o form, não diz nada, e eu não quero andar à procura da template para editar caso a caso"*; (2) *"nos alertas da página inicial aparecem pré-reservas; só deviam aparecer a partir de Entrega agendada, inclusive"*.
- **1. Assistente sem conversa.** O `SuggestComposer` (rascunhos persistentes, histórico `‹2/3›`, afinação, "Abrir no WhatsApp", captura do par gerado/usado) vivia **dentro** do `whatsapp-client.tsx` e só funcionava com uma `conversationId`. Passou a componente próprio, [suggest-composer.tsx](src/components/suggest-composer.tsx), com **dois alvos possíveis**: conversa **ou** encomenda. O workbench da Preservação, no estado "Sem conversa de WhatsApp ainda", passa a mostrá-lo com `orderId` — o assistente lê a encomenda (o mesmo bloco de campos + pontos OBRIGATÓRIOS de sempre) e escreve a **primeira** mensagem. Também aparece quando a encomenda nem sequer tem telefone (aí sem o botão verde do WhatsApp, só Copiar).
- **Efeito colateral bom:** o painel do WhatsApp do workbench (e os das Parcerias e do Vale-Presente) usava um composer **antigo e mais pobre**, duplicado. Deixou de existir: agora é o mesmo componente em todo o lado, com histórico, afinação e aprendizagem incluídos.
- **API:** `POST /api/whatsapp/suggest` aceita `orderId` em vez de `conversationId`. Sem conversa, o transcript é substituído por uma frase explícita — *"o cliente NUNCA escreveu, esta é a PRIMEIRA mensagem"* — porque senão o modelo tende a "responder" a mensagens que não existem. Se por acaso existir conversa com aquele número, as mensagens entram na mesma. `suggest-edit` passa a aceitar pares sem conversa (a coluna `conversation_id` da mig 102 já era nullable) — a aprendizagem funciona igual.
- **2. Alertas do Dashboard.** Regra única em [dashboard.ts](src/lib/dashboard.ts): só entram encomendas de **"Entrega agendada" para a frente** (`isStatusAtOrAfter`, que também exclui canceladas). Cai o alerta "Pré-reserva sem contacto há X dias" — era o que mais enchia o cartão (até 10 linhas vermelhas) e esse acompanhamento já vive na aba Preservação (grupos "Pré-reservas" contactadas/não e "Sem resposta"). Mesma regra que a notificação diária já usava (`SKIP_STATUSES` em [push/daily.ts](src/lib/push/daily.ts)).
- **3. Também por email (pedido dela a meio da sessão).** A aba **Email** do workbench ganha o mesmo assistente, em formato de email: o prompt manda pôr **`Assunto: …` na primeira linha**, saudação e despedida, parágrafos arejados e sem gíria de WhatsApp (os 29 templates são todos de WhatsApp — aproveita-se o conteúdo, muda-se a forma). O botão verde passa a **"Abrir no email"** azul, que abre o programa de email dela com destinatário, assunto e corpo já preenchidos (`mailto:`; acima de ~1900 caracteres o botão desaparece e fica só o Copiar, porque há clientes de email que cortam o corpo em silêncio). Os rascunhos dos dois canais são independentes (chave `email:order:…`), por isso pode ter uma mensagem a meio em cada um. O composer fica **fora** do `GmailPanel` de propósito: aparece mesmo com o Google desligado ou sem emails trocados.
- **Ficheiros:** [suggest-composer.tsx](src/components/suggest-composer.tsx) (NOVO) · [email-subject.ts](src/lib/email-subject.ts) + [email-subject.test.ts](src/lib/__tests__/email-subject.test.ts) (NOVOS, 4 testes) · [whatsapp-client.tsx](src/app/(admin)/whatsapp/whatsapp-client.tsx) (−359 linhas) · [wa-live-panel.tsx](src/app/(admin)/preservacao/[id]/_components/wa-live-panel.tsx) · [comms-card.tsx](src/app/(admin)/preservacao/[id]/_components/comms-card.tsx) · [suggest/route.ts](src/app/api/whatsapp/suggest/route.ts) · [suggest-edit/route.ts](src/app/api/whatsapp/suggest-edit/route.ts) · [dashboard.ts](src/lib/dashboard.ts) · [dashboard-alerts.test.ts](src/lib/__tests__/dashboard-alerts.test.ts) (NOVO, 5 testes).
- **Migrações:** nenhuma. **Preflight ✅** tsc + **190 testes** (eram 181) + build + ESLint.
- **Nota (decisão consciente):** os pares gerado/usado do email registam-se com `used_via = 'copiar'` — o CHECK da mig 102 só aceita `copiar`/`whatsapp` e não vale uma migração nova só para distinguir a origem. A aprendizagem funciona igual.
- **Fica de fora:** o Vale-Presente não tem assistente (o `suggest` é todo construído à volta de uma **encomenda**; um vale precisaria do seu próprio contexto).
- **Correcção pós-deploy (02/09):** na **Caixa de Entrada** o picker mostrava sempre a mesma lista fixa de primeiro contacto, mesmo em conversas de encomendas a meio ("está só a mostrar todas"). Causa: aquela aba sempre usou `scope="lead"` — a lista `orders` que o servidor manda tem só 7 colunas (para o matching por telefone) e o picker precisa da encomenda inteira para sugerir pela fase e preencher variáveis. Passa a ir buscar **a encomenda completa da conversa aberta** (uma linha, no cliente) e a usar `scope="order"`; sem encomenda ligada continua em modo lead, que é o certo para quem escreve antes de preencher o formulário.
- **✅ EM PRODUÇÃO** (02/09, commit `3c70d10`, deploy `dpl_BF1w4i…` READY em admin.floresabeirario.pt). Working tree partilhado com as sessões 157/158 (finanças, vidro museu, flowers-card): committados **só** os ficheiros acima, os delas continuam por committar.
- **🔴 Smoke (por fazer):** (a) encomenda que preencheu o form e nunca escreveu → aba WhatsApp do workbench → "Sugerir mensagem" → sai um primeiro contacto que fala do que ela escolheu (tamanho, fundo, extras) e **não responde a mensagens inexistentes**; (b) Refazer/afinação/histórico funcionam como na Caixa de Entrada; (c) copiar → "✓ Copiado" e o par fica registado; (d) encomenda **sem telefone** → composer aparece na mesma, sem botão verde; (e) conversa já existente → o painel continua igual, agora com afinação e histórico; (f) Dashboard → **nenhuma pré-reserva** nos Alertas, e os eventos/prazos das agendadas continuam lá; (g) aba **Email** do workbench → "Sugerir email" → sai com `Assunto:` na 1.ª linha, saudação e despedida → "Abrir no email" abre o correio com tudo preenchido; (h) encomenda em inglês → email em inglês com `Subject:`.

### Sessão 158 (2026-08-27) — Healthcheck a vermelho: mismatches de hidratação (React #418)
- **O quê:** alerta vermelho no telemóvel da Maria — "Erros na app (últimas 24h): 10". Não eram 10 problemas: **8× React #418** (`args[]=text` = *hydration failed, server rendered text didn't match*) em `/`, `/whatsapp` e `/preservacao/[id]`, mais **2× ruído do supabase-js**. Sites, backup, Google e RPC do vale estavam todos verdes (verificados por fora).
- **Causa:** texto que depende do **relógio** ou do **fuso da máquina**, calculado duas vezes — no servidor (Vercel corre em **UTC**) e no browser (**Lisboa, UTC+1 no verão**). A hora das bolhas do WhatsApp saía em UTC no SSR e em Lisboa ao hidratar → mismatch **em todos os carregamentos**. O `format-date.ts` já tinha sido blindado assim na sessão 129, mas ficaram sítios de fora.
- **Correcção em 3 camadas:** ① helpers novos em [format-date.ts](src/lib/format-date.ts) (`lisbonDayKey`, `calendarDaysFromTodayLisbon`, `formatDateLisbon`, `formatDayMonthLisbon`) e todo o `toLocale*` de datas passa a ter `timeZone: "Europe/Lisbon"`; ② toda a conta de dias ("Hoje"/"Ontem"/"há X dias") passa a usar o **calendário de Lisboa** em vez do da máquina — inclui o `relativeMonthsDays`, que dizia "Amanhã" no servidor e "Hoje" no browser entre a meia-noite de cá e a de Londres; ③ o texto que é **legitimamente** relativo ao relógio ("há 5 min") leva `suppressHydrationWarning` — divergir é o comportamento certo, o React é que tinha de saber.
- **Ruído filtrado:** `Lock "…auth-token" was released because another request stole it` é o supabase-js a serializar a renovação do token com `navigator.locks`; a biblioteca repete sozinha. Passa a ser ignorado em [error-reporter.tsx](src/components/error-reporter.tsx). **A lista de ignorados é curta de propósito** — chunks que falham a carregar e TypeErrors têm de continuar a chegar.
- **Ficheiros:** [format-date.ts](src/lib/format-date.ts) · [error-reporter.tsx](src/components/error-reporter.tsx) · [layout.tsx](src/app/(admin)/layout.tsx) (tooltip do healthcheck mostrava hora UTC) · [whatsapp-client.tsx](src/app/(admin)/whatsapp/whatsapp-client.tsx) · [wa-live-panel.tsx](src/app/(admin)/preservacao/[id]/_components/wa-live-panel.tsx) · [gmail-panel.tsx](src/app/(admin)/preservacao/[id]/_components/gmail-panel.tsx) · [alerts.tsx](src/app/(admin)/preservacao/[id]/_components/alerts.tsx) · [shared.ts](src/app/(admin)/preservacao/[id]/_components/shared.ts) · [shipping-card.tsx](src/app/(admin)/preservacao/[id]/_components/shipping-card.tsx) · dashboard: [format-helpers.ts](src/app/(admin)/_components/dashboard/format-helpers.ts) + pickups-card + recent-done-row + tasks-card · [format-date-lisbon.test.ts](src/lib/__tests__/format-date-lisbon.test.ts) (NOVO, 5 testes).
- **Bug de lambuja:** o `formatRelativeDays` das recolhas usava `differenceInDays` (blocos de 24h a partir de agora), por isso uma recolha **amanhã de manhã** dizia "Hoje". Passa a contar dias de calendário.
- **Como se prova (não dá para smoke normal):** o teste novo corre nos **dois fusos** e tem de dar o mesmo: `TZ=UTC npx vitest run src/lib/__tests__/format-date-lisbon.test.ts` e `TZ=Europe/Lisbon …`. Ambos ✅.
- **Migrações:** nenhuma. **Preflight ✅** tsc + build + **181 testes** (eram 176).
- **⚠️ POR COMMITAR/DEPLOY.** A sessão 157 tem ficheiros por committar no mesmo working tree — **sem sobreposição** com estes. Só depois do deploy é que os erros param de entrar.
- **Smoke:** (a) `/whatsapp` → a hora das bolhas é a de Portugal (não menos 1h); (b) `/preservacao/<id>` com conversa e emails → mesma coisa nos dois painéis; (c) Dashboard → uma recolha marcada para amanhã diz "Amanhã" e não "Hoje"; (d) Healthchecks → o cartão "Erros na app" lista os erros com `N×`, página e stack colapsado (enquanto os registos antigos lá estiverem); (e) passadas 24h do deploy, esse cartão volta a verde.
- **Ficou de fora (mesmo bug, sítio não implicado nos erros):** o contador do congelador em `flowers-card.tsx` conta dias no fuso da máquina — não se lhe tocou porque o ficheiro tem alterações da sessão 157 por committar e misturá-los estragava o commit.
- **Parte 2 (aprovada por ela na mesma sessão) — os erros passam a ver-se dentro do cartão:** até agora o cartão "Erros na app" dizia só *"10 erros"* e mandava correr `SELECT * FROM client_errors` no SQL Editor. No telemóvel isso é o mesmo que não saber nada, e foi exactamente o que aconteceu hoje. O check passa a trazer as **5 famílias mais frequentes** (agrupadas por mensagem+página, porque N registos são quase sempre o mesmo erro repetido), cada uma com `N×`, a mensagem, a página, a última vez e o **stack num `<details>` fechado** — minificado não diz nada a ela, mas é o que o Claude precisa de ler.
- **Ficheiros (parte 2):** [healthchecks.ts](src/lib/healthchecks.ts) (tipo `HealthCheckSample` + agregação) · [healthchecks/page.tsx](src/app/(admin)/healthchecks/page.tsx) · [healthchecks-client.tsx](src/app/(admin)/healthchecks/healthchecks-client.tsx) (`SampleRow`).
- **Nota:** as amostras **não** entram no `summariseHealthchecks`, portanto a cache em `system_settings` e a bolinha da sidebar não incham. `bg-cocoa-100` não existe na paleta (só 500/700/900) — usar `bg-cream-100`.

### Sessões 157 e 156 — movidas na íntegra para o [PROGRESS-ARQUIVO.md](PROGRESS-ARQUIVO.md) (sessão 162)
- **157** (2026-08-26): vidro museu passa a opção paga nos 2 repos + preços do site ligados à BD (migs 104/105 corridas; em produção 06/09).
- **156** (2026-08-21): templates listam os extras + mensagem pública editável no workbench (mig 103 corrida).

## Pendências externas (outros repos)

Tudo o que está aqui **já está em produção**; o detalhe vive nos commits. Só falta o que está marcado.

- **fbr-voucher** — 3 levas feitas (cartão 3D em WebP −94%, fontes locais/RGPD, versão EN com selector, envelope personalizado + pétalas + partilhar, OG image, código de exemplo `EXEMPLO`/`EXAMPLE`). **Falta:** smoke visual da Maria em `/EXEMPLO` (PT e EN) e num vale real no telemóvel. **Não feito de propósito:** Umami no voucher (à espera da palavra dela).
- **fbr-website** — links "Ver um exemplo do vale digital" no vale-presente e em `/oferecer-preservacao` (live). Relatório mensal do Clarity ✅ automatizado (cron + Resend). **Em aberto (auditoria 122):** `aggregateRating`? subtítulo no hero? data nas páginas legais? vídeo `tracking.mp4` (a Maria ainda não tem). Umami continua manual (API paga) — [[project_website_analytics]].
- **Análise mercado/conversão/blog do fbr-website (06/07)** — concorrentes PT, estratégia resina, prova social em falta nas páginas de decisão, 6 artigos propostos. **POR IMPLEMENTAR** ([[project_website_mercado_conversao_2026-07]]).
- **⚠️ Sessões paralelas:** este working tree é partilhado com outras sessões Claude. `git status` antes de commitar, sempre ([[project_parallel_sessions_worktree]]). Hoje houve colisão de numeração (duas sessões chamaram-se 152).

---

## Próximas frentes (por ordem — ver "Próximo passo concreto" no topo)

- Varrimento `formatDateTimeLisbon` (129) → tipos gerados Supabase no preflight → vista "Hoje" + relatório mensal → expurgo WhatsApp do git (sessão dedicada) → cadência de comunicação (104)
- **Alertas por email no link de status** (a Maria fá-lo quando tiver tempo, sessão 147): cliente opta por receber avisos no `status.floresabeirario.pt` (email + consentimento RGPD numa tabela `status_subscriptions` nova, com GRANT/RLS); no admin (aba Status) um botão **"Notificar cliente desta atualização"** que **só envia quando a Maria carrega** (mudanças de estado acidentais nunca enviam nada); email PT/EN via Resend. Aprovado o approach "email opt-in + botão manual" (nunca automático — [[feedback_nada_de_envio_automatico]]).
- **Calculadora do custo da recolha** (adiado pela Maria em 06/09/2026, sessão 160): a partir da morada de recolha já geocodificada no formulário, calcular km ida e volta × €/km + **portagens reais** + tempo × €/h + margem. Portagens têm de ser **valores concretos, não estimativas por km** (ela usa a ViaMichelin, que não tem API pública) → usar a **Routes API da Google** (`computeRoutes` com `extraComputations: ["TOLLS"]`, devolve `travelAdvisory.tollInfo` em EUR para Portugal; chave já existe no Google Cloud dela). Faltam 3 números dela (€/km, €/hora, valor mínimo). Começar **só no admin** (cartão da recolha no workbench) e comparar meia dúzia de casos com a ViaMichelin antes de mostrar ao cliente no resumo.
- **Chat interno — media** (upload foto/vídeo/áudio; hoje só texto)
- **Mover Competição** de Finanças para Parcerias (decidir: sub-aba ou aba "Inteligência")
- View SQL `order_pnl` para exports/queries ad-hoc (nice-to-have)
- Outras ideias vivem na aba `/ideias` da plataforma

---

## Armadilhas conhecidas (anti-repetição)

- **timestamptz → sempre `formatDateTimeLisbon`** (nunca `format(…HH:mm)` do date-fns em componentes hidratados — React #418, sessão 129)
- **`useEffect+setState` viola ESLint** — usar "store info from previous renders" ([[feedback_react_set_state_in_effect]])
- **`useSyncExternalStore` snapshot** tem de devolver referência cacheada ou dá React #185 ([[feedback_useSyncExternalStore_pitfall]])
- **`INSERT...RETURNING` precisa de GRANT SELECT** — não só INSERT ([[feedback_supabase_rls_pitfalls]]); tabelas novas precisam de GRANT explícito ([[project_supabase_public_grants_2026]])
- **`CREATE TABLE IF NOT EXISTS` é silencioso** se a tabela existe — usar `ALTER TABLE` em migrações subsequentes
- **Migrações no repo ≠ aplicadas em produção** — é a Maria que as corre no SQL Editor; verificar antes de depender ([[feedback_migracoes_supabase_aplicadas]])
- **Vercel não auto-redeploya** ao mudar env vars — forçar; `NEXT_PUBLIC_*` só entra no build seguinte
- **Nada entra em `public/`** que não seja para servir na app (tudo aí é público no deploy)
- **Sessões paralelas** no mesmo working tree — `git status` antes de commitar ([[project_parallel_sessions_worktree]])
- **base-ui (não Radix):** `PopoverTrigger` sem `asChild` — o Trigger já é `<button>`
- **Refs não se tocam durante o render** — o reset por mudança de props corre no render, e aí `ref.current = x` viola `react-hooks/refs`. Usar `useState` (sessão 153). Primo do [[feedback_react_set_state_in_effect]]
- **Estado em memória não sobrevive à PWA ser morta** — o Android/iOS mata a app em segundo plano; qualquer coisa que a Maria esteja a escrever tem de ir para localStorage a cada tecla, não só ao submeter (sessão 153)
- **Exemplos few-shot uniformes ensinam padrões que ninguém escreveu** — anonimizar as mensagens dela (`{nome}`) fez o modelo aprender que as mensagens da Maria não levam nome. Ao mudar dados de exemplos, perguntar sempre que ESTILO isso ensina (sessão 153)
- **`onClick={fn}` passa o evento como 1.º argumento** — inofensivo até a função ganhar um parâmetro; usar `onClick={() => fn()}` quando houver hipótese de crescer (sessão 153)
- **Cada linha nova no rodapé do WhatsApp custa conversa visível** — o rodapé passou dos 500px e tapava as mensagens no telemóvel; tectos de altura relativos ao ecrã, não fixos ([[feedback_simplificar_antes_de_redesenhar]])
- **CSP bloqueia domínios externos novos e é INVISÍVEL ao build** — ao adicionar qualquer script/imagem/fetch de um domínio novo no fbr-website, actualizar a `Content-Security-Policy` em `next.config.mjs`. Nem `next build`, nem `curl`, nem validar a chave apanham isto: só um browser real (sessão 155)
- **`vitest run` falha esporadicamente no Windows** com "13 failed / no tests" — é flakiness do pool, não regressão; correr outra vez antes de investigar (visto na 146 e na 153)
- **Smoke test obrigatório** antes de fechar sessões que mexem em páginas críticas ([[feedback_smoke_test_obrigatorio]])
- **Ecos do WhatsApp (mensagens que a Maria envia do telemóvel) chegam com atraso** — vêm por um canal separado da Meta (`smb_message_echoes`), mais lento que as mensagens das clientes (`messages`), e agora com o salto extra do Dualhook. A aba `/whatsapp` É tempo real (Supabase Realtime, INSERT em `whatsapp_messages`), por isso aparecem sozinhas quando chegam. Se uma mensagem enviada do telemóvel demora segundos/1-2min a aparecer, é o eco lento da Meta, **não um bug** — o webhook e a inserção estão OK (confirmado 09/08: 200 + zero erros nos logs). Só investigar se uma mensagem de **cliente** falha, ou se demora muitos minutos / nunca chega.
