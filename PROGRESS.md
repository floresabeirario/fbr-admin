# FBR Admin — Estado do Projecto

> Lido no início de cada sessão; actualizado em tempo real durante a sessão.
> **Regras deste ficheiro:** máximo ~30 KB. Só as últimas 5 sessões ficam aqui, em formato compacto
> (template: O quê / Ficheiros / Migrações + passos manuais / Smoke / Pendente, máx ~15 linhas).
> Ao entrar a 6ª sessão, a mais antiga move-se **na íntegra** para o [PROGRESS-ARQUIVO.md](PROGRESS-ARQUIVO.md)
> (que NÃO é lido por defeito — todo o histórico das sessões 1-132 está lá). O detalhe fino vive nos commits do git.
> ⚠️ Hashes de commits do fbr-admin anteriores a 11/07/2026 foram reescritos no expurgo RGPD (sessão 139) — já não existem.

---

## Onde estamos

**Fase 6 — Integrações + PWA + RGPD (em curso).** Última sessão: **153** (2026-08-17, assistente do WhatsApp passa a ler os campos da encomenda + a escrever na voz real da Maria + UX no telemóvel — detalhe no bloco abaixo). A 152 correu **em paralelo** no mesmo dia (converter encomenda para "flores secas"); ambas em produção. Sessão 145 (Emoldurar Flores Secas) já em produção desde 26/07.

### ⚠️ Pendentes de confirmação da Maria (verificar antes de assumir)
- [ ] **Sessão 153c — Composer do WhatsApp sobrevive à app ser morta + histórico de sugestões (SEM migração):**
  - **Bug 1 (reportado pela Maria):** *"vou para outra app e volto, o admin reinicia e perco a mensagem que estava a escrever"*. Causa: o Android/iOS mata a PWA em segundo plano para libertar memória; ao voltar remonta do zero e o estado em memória (React `useState`) desapareceu. **Agravado pelo botão "Abrir no WhatsApp" da 153**, que é precisamente o que a manda para fora da app.
  - **Bug 2:** *"não dá para ver histórico de sugestões"*. O "Refazer" substituía a sugestão anterior sem volta — se a 1.ª estava melhor, perdia-se.
  - **Correcção (uma solução para os dois):** [lib/whatsapp/composer-drafts.ts](src/lib/whatsapp/composer-drafts.ts) NOVO — store `useSyncExternalStore` + localStorage no padrão já usado em [preservacao-views.ts](src/lib/preservacao-views.ts) (snapshot cacheado pelo valor cru, server snapshot vazio para não haver hydration mismatch, [[feedback_useSyncExternalStore_pitfall]]). O texto e a instrução passam a ser gravados **a cada tecla**, por conversa. Cada geração **empilha** um rascunho (até 5) em vez de substituir; navegação `‹ 2/3 ›` no cabeçalho da sugestão. Tectos: 5 rascunhos/conversa, 20 conversas (despeja as mais antigas por `updatedAt`), 8000 chars. O X limpa só aquela conversa.
  - **`used` por rascunho** (em vez do flag em memória da 153b): sobrevive ao reload, por isso a app morrer entre copiar e voltar já não faz registar o mesmo par duas vezes na `suggestion_edits`.
  - **Ficheiros:** [composer-drafts.ts](src/lib/whatsapp/composer-drafts.ts) NOVO, [composer-drafts.test.ts](src/lib/__tests__/composer-drafts.test.ts) NOVO (12 testes), [whatsapp-client.tsx](src/app/(admin)/whatsapp/whatsapp-client.tsx). **Preflight:** ✅ tsc + **146 testes** + build + ESLint limpo.
  - **Smoke (Maria):** escrever meia mensagem → ir ao WhatsApp/outra app → voltar ao admin → o texto tem de estar lá tal e qual; carregar em "Refazer" 2-3 vezes → aparece `‹ 2/3 ›` no cabeçalho e dá para voltar às anteriores; fechar no X → esquece; abrir outra conversa → cada uma tem o seu rascunho.

- [ ] **Sessão 153b — Ciclo de aprendizagem do assistente (⚠️ TEM MIGRAÇÃO POR CORRER):**
  - **🔴 PASSO MANUAL 1 (ANTES do deploy):** correr [`102_suggestion_edits.sql`](supabase/migrations/102_suggestion_edits.sql) no SQL Editor do Supabase. Cria a tabela `suggestion_edits` (+ RLS `is_team_admin`, GRANTs explícitos a `authenticated` e `service_role` — tabela nova, [[project_supabase_public_grants_2026]]) e a chave `claude_voice_rules` em `system_settings`. Queries de verificação no fim do ficheiro. **Sem ela nada rebenta** (a captura é best-effort e falha em silêncio), mas não se aprende nada e a secção nova avisa em âmbar que falta correr.
  - **Porquê:** a Maria gerava a sugestão, corrigia-a e copiava — e esse par (o que ele escreveu → o que ela enviou) era deitado fora de cada vez. O assistente nunca melhorava com o uso. Era o que ela invejava na memória do ChatGPT.
  - **Captura (silenciosa, zero cliques novos):** ao carregar em **Copiar** ou **Abrir no WhatsApp**, a rota nova [suggest-edit/route.ts](src/app/api/whatsapp/suggest-edit/route.ts) guarda o par. `edited` calculado com espaços normalizados (senão quase tudo contava como editado). Guarda-se **uma vez por sugestão** (copiar + WhatsApp = a mesma mensagem). Pares **não editados também contam**: dizem o que já está bom, sem eles a análise só via defeitos. Respeita [[feedback_menos_ruido_workflow]]: nada de botões ou registos extra.
  - **Análise (a pedido, nunca automática):** [voice-rules/route.ts](src/app/api/whatsapp/voice-rules/route.ts) manda até 40 pares ao Claude a perguntar *"que regras teriam feito a 1ª versão já ser a 2ª?"*; devolve no máximo 5 regras com o porquê e em quantos pares foi vista. Mínimo de 5 pares para analisar. Os pares ficam marcados `analysed_at` (não repetem propostas). Custo logado no `claude_usage` como o resto.
  - **Aprovação:** secção nova "Aprender com as tuas edições" no Cérebro do Claude ([claudio-client.tsx](src/app/(admin)/comunicacoes/claudio/claudio-client.tsx)) com cartões **Aceitar / Rejeitar** por regra. As aceites caem na caixa "Regras em vigor" (editável à mão) e, ao guardar, vão para `system_settings.claude_voice_rules`, que entra no system prompt **depois dos factos e acima de tudo o resto** (foram tiradas do que ela escreve mesmo). Separada de `claude_persona` de propósito: a persona é escrita por ela, isto é aprendido.
  - **Ficheiros:** mig 102 NOVA, [suggest-edit/route.ts](src/app/api/whatsapp/suggest-edit/route.ts) NOVA, [voice-rules/route.ts](src/app/api/whatsapp/voice-rules/route.ts) NOVA, [suggest/route.ts](src/app/api/whatsapp/suggest/route.ts) (injecta as regras), [whatsapp-client.tsx](src/app/(admin)/whatsapp/whatsapp-client.tsx) (captura), [claudio-client.tsx](src/app/(admin)/comunicacoes/claudio/claudio-client.tsx) + [page.tsx](src/app/(admin)/comunicacoes/claudio/page.tsx) (UI), [message-template.ts](src/types/message-template.ts) + 3 sítios com defaults (chave `claude_voice_rules`).
  - **Armadilha apanhada pelo ESLint:** o flag "já registei" era um `useRef`, mas o reset por mudança de conversa corre **durante o render** e aí um ref não pode ser tocado (`react-hooks/refs`). Passou a `useState`. Mesma família de [[feedback_react_set_state_in_effect]].
  - **Preflight:** ✅ tsc + 134 testes + build + ESLint limpo.
  - **Smoke (Maria, DEPOIS da migração):** usar o assistente 5+ vezes (copiar ou abrir no WhatsApp) → ir a Comunicações → Cérebro do Claude → a secção violeta deve dizer "N mensagens por analisar" → "Analisar as minhas edições" → aparecem propostas → aceitar uma → "Guardar" → gerar nova sugestão e ver se a regra é respeitada.

- [ ] **Sessão 153 — Assistente lê a encomenda + escreve na voz da Maria + UX no telemóvel (SEM migração, EM PRODUÇÃO 17/08, `cac50e6`, deploy Vercel READY — falta só o smoke da Maria):**
  - **Porquê:** a Maria não se relacionava com o que o assistente escrevia ("não conhece a minha voz") e continuava a reescrever tudo à mão. Diagnóstico: (a) o prompt nunca teve uma única mensagem real dela, só a persona escrita à mão + os 29 templates; (b) a query do `suggest` só passava 22 colunas e **omitia** ornamentos/pendentes/quadros extra/extras/tipo de flores, logo respondia às cegas a quem tinha marcado "Mais info" no formulário.
  - **Parte 1 — o assistente vê a encomenda (A1-A5 aprovados; A6 fundo NÃO):** [suggest/route.ts](src/app/api/whatsapp/suggest/route.ts) passa a ler `flower_type`, `extras_in_frame` (opções + texto livre), `christmas_ornaments(_qty)`, `necklace_pendants(_qty)`, `extra_small_frames(_qty)`. Valores "Mais info" vão marcados `← PENDENTE`.
  - **Parte 2 — conteúdos obrigatórios (B1-B5 aprovados; B6 fundo NÃO; SEM preços por decisão da Maria):** `requiredContentPoints()` NOVO em [templates.ts](src/lib/templates.ts). Diferente do `fieldSuggestionBases` (que *sugere* templates): isto diz o que a mensagem **TEM** de conter e entra no prompt como secção "OBRIGATÓRIO". Regras: ornamentos/pendentes/quadros extra em "mais_info" → explicar + perguntar se quer incluir (nunca preços); envio das flores "não sei"/vazio → 3 opções; recolha sem morada → pedir morada. Estados `quadro_recebido`/`cancelado` não geram pendências.
  - **Parte 3 — voz aprendida das mensagens reais:** [lib/whatsapp/voice-examples.ts](src/lib/whatsapp/voice-examples.ts) NOVO. Vai buscar `whatsapp_messages` com `direction='sent_echo'` (o eco das mensagens que a Maria escreve no telemóvel), filtra o que não ensina estilo (<80 ou >1200 chars, só-emoji, só-link), **anonimiza todos os nomes conhecidos → `{nome}`** (crítico: os exemplos vêm de conversas com OUTRAS clientes) e escolhe os 8 mais parecidos com a situação (palavras em comum + bónus de recência, meia-vida ~90 dias). Vai no bloco do utilizador, **não** no system, para não estragar o cache dos templates/persona. Auto-alimenta-se: ela escreve hoje no telemóvel, o assistente usa hoje. Sem treino de modelo, sem job, sem migração.
  - **Ficheiros:** [suggest/route.ts](src/app/api/whatsapp/suggest/route.ts), [templates.ts](src/lib/templates.ts), [voice-examples.ts](src/lib/whatsapp/voice-examples.ts) NOVO, [templates.test.ts](src/lib/__tests__/templates.test.ts), [voice-examples.test.ts](src/lib/__tests__/voice-examples.test.ts) NOVO. **Migrações: nenhuma.** Modelo mantém-se `claude-sonnet-4-6` (a Maria não aprovou troca).
  - **Correcção pós-deploy (`602ec8f`, ✅ confirmada pela Maria em produção):** a sugestão saía **sem tratar a cliente pelo nome**, intermitente (numa conversa sim, noutra não). Causa: os exemplos de voz vão todos anonimizados (`{nome}`), o que sem querer ensinava ao modelo que as mensagens da Maria não levam nome. Correcção em 3 camadas: (a) o primeiro nome DESTA conversa é injectado nos exemplos (o sinal de estilo passa a estar certo; a anonimização continua intacta, o nome da cliente anterior nunca lá chega); (b) o nome vem de `client_name` da encomenda ligada, com fallback para `contact_name` — sem nenhum, o prompt manda escrever sem nome e proíbe inventar; (c) `preencherNome()` limpa em código qualquer `{nome}` que escape, incluindo a pontuação pendurada ("Bom dia, {nome}!" → "Bom dia!"). **Lição:** exemplos few-shot uniformes ensinam padrões que ninguém escreveu de propósito — anonimizar o corpus mudou o estilo, não só os dados.
  - **Preflight:** ✅ tsc + **134 testes** (eram 110) + build.
  - **Smoke (Maria):** abrir conversa de cliente com ornamentos/pendentes em "Mais info" ou envio "Não sei" → "Sugerir resposta" deve explicar esses pontos sozinha e **sem preços**; e o tom deve soar mais a ela. Confirmar que **nenhum nome de outra cliente** aparece na sugestão (a anonimização tem testes, mas vale ver em produção).
  - **Parte 4 — UX/UI do WhatsApp no telemóvel (U1-U4, a Maria aprovou "tudo"):**
    - **U1 — bug do toast "Copiado" que não saía e tapava os botões.** Causa real (3 coisas juntas): o [sonner.tsx](src/components/ui/sonner.tsx) não definia `position`, logo no telemóvel o toast desenha-se EM BAIXO à largura toda, mesmo por cima do rodapé Copiar/Refazer; não tinha `closeButton`; e **num ecrã táctil o toque conta como hover, o `mouseleave` nunca chega e o sonner deixa o auto-fechar pausado para sempre** (comportamento do componente, não bug nosso). Correcção dupla: (a) o "Copiado" deixa de ser toast e passa a ser o próprio botão a mudar para "✓ Copiado" 2s (mesmo padrão do template-picker); (b) no telemóvel (`useIsMobile`, 768px) os toasts restantes vão para `top-center`, com X e 3s. **Desktop intocado** (`bottom-right`, 4s, sem X).
    - **U2 — "Abrir no WhatsApp" (o maior ganho no telemóvel).** Botão verde que abre `wa.me/<número>?text=<mensagem>`: cai directamente na conversa certa com o texto já na caixa de escrita, só falta carregar em enviar. Mata o ciclo copiar → sair da PWA → procurar a conversa → colar. **Não viola [[feedback_nada_de_envio_automatico]]:** é sempre a Maria a carregar em enviar, dentro do WhatsApp. Copiar fica ao lado como alternativa (`variant="outline"` quando o botão verde existe).
    - **U3 — alvos de toque.** Botões do composer passam de `h-8` (32px, abaixo dos 44px recomendados) para `h-11 lg:h-8`; X de fechar com área de toque maior. Desktop intocado (`lg:`).
    - **U4 — caixa da sugestão cresce com o texto** (`autoGrow`, tecto 320px) em vez de 6 linhas fixas, que com o teclado aberto mostravam uma nesga. O `Textarea` já tinha `field-sizing-content` (nativo só em Chromium); o JS dá o mesmo comportamento no iOS e acrescenta o tecto. ⚠️ **Efeito colateral no desktop:** a caixa passou a `resize-none` (a pega manual deixa de fazer sentido com auto-grow, seria sempre reposta ao teclar). Se a Maria a quiser de volta, reverte-se.
    - **U5 NÃO feito (fora desta leva):** rascunho pré-gerado ao abrir a conversa (tem custo de API e mais decisões de desenho).
  - **Não feito (a Maria não aprovou / por decidir):** textos canónicos de explicação dos extras aprovados por ela (hoje o Claude explica por palavras dele); guia de voz destilado semanal; captura das edições dela ao carregar em Copiar (ciclo de aprendizagem); rascunhos pré-gerados (U5); botão de microfone. Análise de custos da API feita na sessão (Haiku 4.5 ~2,50€/mês vs Sonnet ~9€/mês vs Opus 5 ~19,50€/mês a 10 sugestões/dia).

- [ ] **Sessão 152 — Converter uma encomenda para "flores secas" + cor própria do serviço (EM PRODUÇÃO 17/08, `2dc4a7c`, deploy Vercel READY — falta só o smoke da Maria):**
  - **Porquê:** noiva reservou preservação mas o ramo já vinha quase todo seco — não faz sentido cobrar preços de preservação.
  - **O quê:** o select "Tipo de serviço" no rodapé do workbench passa a ter os **3** tipos (secas deixa de ser selo read-only). Trocar de/para secas abre **diálogo de confirmação** que explica: preços passam para a tabela `secas_*`, orçamento recalcula-se **só se ainda for o automático** (editado à mão fica intocado, com dica do botão "Recalcular"), "Flores na prensa" sai/entra na timeline pública e no selector, campos das secas ficam por preencher, e aviso extra se a encomenda estiver mesmo em "Flores na prensa". Preservação ↔ Recriação continua instantâneo (mesmos preços).
  - **Recalculo:** [actions.ts](src/app/(admin)/preservacao/actions.ts) — `service_type` entra no `needsPrev` e no `pricingFieldChanged` (só recalcula quando muda de valor e o orçamento == snapshot).
  - **Cor (2 rondas):** a Maria disse que na lista o selo SECAS, "Entrega agendada" e "30% pago" eram todos âmbar; **cores de estado/pagamento ficam intocadas** (decisão dela, sem bolinhas). 1.ª tentativa verde-esmeralda → **batia com a pílula verde "Contactada"** na mesma linha. Final: **selo SECAS em castanho da marca** (`bg-cocoa-900 text-cream-50`, o único chip escuro da linha, não colide com nenhuma pílula pastel) no selo da lista, aba do filtro e tag do cabeçalho; o **cartão do workbench fica verde** (lá não há pílulas a competir; é a cor do form de secas no site).
  - **Ficheiros:** [closing-cards.tsx](src/app/(admin)/preservacao/[id]/_components/closing-cards.tsx) (`ServiceChangeDialog` novo + MetaFooter), [actions.ts](src/app/(admin)/preservacao/actions.ts), [preservacao-client.tsx](src/app/(admin)/preservacao/preservacao-client.tsx), [header.tsx](src/app/(admin)/preservacao/[id]/_components/header.tsx), [dried-flowers-card.tsx](src/app/(admin)/preservacao/[id]/_components/dried-flowers-card.tsx). **Sem migração.** Preflight ✅ (tsc + 110 testes + build).
  - **Smoke (Maria):** abrir a encomenda da noiva → rodapé "Tipo de serviço" → "Emoldurar flores secas" → ler o diálogo → confirmar → orçamento cai para o preço das secas (se não estava editado à mão), tag verde "SECAS" no cabeçalho e na lista, cartão verde das secas aparece, dropdown de estado já sem "Flores na prensa"; site de acompanhamento dessa encomenda sem esse passo.
- [ ] **Sessão 151 — Sinalização "no desidratador" (só interno) + "na prensa há X" + cards Concluídos/Cancelados colapsados:** sistema para saber que encomendas estão neste momento no desidratador (fase Preservação e design).
  - **Migração 099** ([`099_orders_in_dehydrator.sql`](supabase/migrations/099_orders_in_dehydrator.sql)) — ✅ **CORRIDA** (Maria) (coluna `orders.in_dehydrator boolean not null default false`; grants table-level herdam, `orders` anterior a 30/10/2026).
  - **Migração 100** ([`100_orders_flores_na_prensa_at.sql`](supabase/migrations/100_orders_flores_na_prensa_at.sql)) — ✅ **CORRIDA** (Maria, sessão 151). Coluna `orders.flores_na_prensa_at timestamptz` + **CREATE OR REPLACE do trigger `sync_public_status_fields`** (mig 005, o único que o define) para carimbar o momento de entrada em "flores_na_prensa" (mesma condição da data prevista +6m) + **backfill** `flores_na_prensa_at = estimated_delivery_date - 6 meses` (a data prevista é gerada nessa transição, por isso recupera a entrada). Sem GRANT novo.
  - **Migração 101** ([`101_orders_in_dehydrator_at.sql`](supabase/migrations/101_orders_in_dehydrator_at.sql)) — ✅ **CORRIDA** (Maria, sessão 151). Coluna `orders.in_dehydrator_at timestamptz` + trigger `sync_dehydrator_timestamp` (carimba na entrada, limpa na saída, cobre os 2 caminhos: cards e workbench) + backfill das que já estão marcadas. É a base do contador **"No desidratador há X"** (a Maria corrigiu: quer o tempo desde que foi posta no desidratador, NÃO desde a prensa).
  - **Comportamento:** o botão/​selo do desidratador só aparece na **janela de ~1 mês** após entrar na prensa (`isWithinDehydratorWindow`, usa `flores_na_prensa_at`); fora dela mantém-se só se ainda estiver marcado. Info **"No desidratador há X dias e X horas"** (`formatElapsedSince(in_dehydrator_at)`) no card e no cabeçalho do workbench — só quando está marcada.
  - **Ficheiros:** [database.ts](src/types/database.ts) (campos + `isPreservacaoDesignStatus`/`isWithinDehydratorWindow`/`formatElapsedSince`), [actions.ts](src/app/(admin)/preservacao/actions.ts) (`setOrderInDehydratorAction` + safeguard que limpa o flag ao sair da fase de design), [preservacao-client.tsx](src/app/(admin)/preservacao/preservacao-client.tsx) (cards: selo laranja + toggle + "no desidratador há" + Concluídos/Cancelamentos colapsados por defeito), [header.tsx](src/app/(admin)/preservacao/[id]/_components/header.tsx) (workbench). Só interno, nada no site público, nada automático.
  - **Ordem dos cards (Maria pediu):** botão na vista Cards para inverter — "Ordem por defeito" ↔ "Mais recentes primeiro" (por `created_at` desc dentro de cada grupo; grupos ficam na mesma ordem). Escolha **persistida** em `cardsNewestFirst` no store [preservacao-views.ts](src/lib/preservacao-views.ts). Selo do card passou a "No desidratador".
  - **Deploys:** `672bb43` (base) + `64bc2cc` (janela 1 mês) já em produção. 3.º (contador "no desidratador há", mig 101) + 4.º (ordem dos cards + wording) neste push.
  - **Smoke (Maria):** vista Cards → pôr um card no desidratador → fica laranja/selo + "No desidratador há X minutos"; passa dias → "X dias e Y horas"; tirar → contador some; abrir a encomenda → botão + "no desidratador há" no cabeçalho; encomenda >1 mês na prensa e sem flag → sem botão; Concluídos/Cancelamentos colapsados.
- [ ] **Sessão 149 — Novo serviço "Recriação" (só admin, EM PRODUÇÃO 10/08, admin `46c944d` READY):** carimbo interno numa encomenda que veio pelo form normal de preservação (mesmos preços/estados/status — decisões da Maria: preços iguais 300/400/500€, acompanhamento público igual, **nada no fbr-tracking**). **Migração 098** [`098_service_type_recriacao.sql`](supabase/migrations/098_service_type_recriacao.sql) ✅ **CORRIDA** (Maria confirmou, sessão 150). Ficheiros: [database.ts](src/types/database.ts) (ServiceType + label), [preservacao-client.tsx](src/app/(admin)/preservacao/preservacao-client.tsx) (badge violeta + filtro dinâmico), [closing-cards.tsx](src/app/(admin)/preservacao/[id]/_components/closing-cards.tsx) (**MetaFooter**: select "Tipo de serviço" discreto no rodapé do workbench, flores secas = selo read-only). A tag violeta "Recriação" no cabeçalho junto ao ID **já estava committada** num commit anterior da Maria ([header.tsx](src/app/(admin)/preservacao/[id]/_components/header.tsx) L191-195, série do workbench mobile) — por isso não veio no diff desta sessão; confirmada visível em produção. **Smoke (Maria):** abrir uma encomenda → tag "Recriação" no cabeçalho + rodapé "Tipo de serviço" → mudar para Recriação → badge violeta na lista + aba "Recriação" no filtro segmentado.
- [ ] **Status público das flores já secas esconde "Flores na prensa" (2 repos, EM PRODUÇÃO 10/08):**
  - **Porquê:** flores já secas não passam pela prensa; a timeline pública mostrava sempre os 12 passos (fase 3 = "Flores na prensa"), confuso para quem enviou secas.
  - **Migração 097** (`097_public_status_service_type.sql`) — **✅ VERIFICADA APLICADA** (sessão 150: o anon já lê `orders.service_type` via REST → o GRANT da 097 está em produção). Dá `GRANT SELECT (service_type)` ao anon + `CREATE OR REPLACE` da RPC `get_public_order_status` para devolver também `service_type`. O teste `public-status-sync.test.ts` foi reapontado para a 097 (definição viva).
  - **fbr-tracking (`main` `854998a`):** [timeline.js](../fbr-tracking/fbr-tracking/fbr-tracking/utils/timeline.js) passos ganham nº de fase + `stepsForService()` (esconde fase 3 nas secas, renumera sozinho sem buracos); [supabase.js](../fbr-tracking/fbr-tracking/fbr-tracking/utils/supabase.js) devolve `tipo_servico` (fallback `preservacao` se ausente); [[id].js](../fbr-tracking/fbr-tracking/fbr-tracking/pages/[id].js) usa a lista filtrada e conta "Etapa X de Y" pelos passos visíveis.
  - **Admin (`46c944d`):** o selector de estado esconde "Flores na prensa" nas encomendas de flores secas. Helper único `isStatusHiddenForService()` em [_styles.ts](src/app/(admin)/preservacao/_styles.ts) (`HIDDEN_STATUSES_BY_SERVICE`), usado pelos DOIS selectores: workbench ([fields.tsx](src/app/(admin)/preservacao/[id]/_components/fields.tsx) `StatusSelect` + prop `serviceType`) e lista ([preservacao-client.tsx](src/app/(admin)/preservacao/preservacao-client.tsx) `StatusSelect`, passado por `OrderRow`). Grupos que ficariam vazios são omitidos. (Preservação/recriação: intactas.)
  - **Smoke (Maria):** (site) status de encomenda de flores secas → timeline sem "Flores na prensa", passos 1..11 sem saltos, "Etapa X de 11"; preservação normal → 12 passos com "Flores na prensa". (admin) abrir/lista de encomenda secas → dropdown de estado sem "Flores na prensa"; encomenda de preservação → continua a ter.
- [ ] **Sessão 148 — Templates de pré-reserva "indeciso" + bug do desconto no orçamento:**
  - **Bug corrigido (código, entra no deploy):** as mensagens não adaptavam quando a Maria editava o orçamento à mão (ex.: desconto de família). Causa: [templates.ts](src/lib/templates.ts) fazia `total = pricing_snapshot.total ?? budget` — o snapshot ganhava ao orçamento editado. Agora **o orçamento manda** (`budget ?? snapshot.total`), e `{valor_quadro}` absorve a diferença (moldura+extras = orçamento). Afecta `{valor_sinal}` `{valor_2a/3a_parcela}` `{valor_total}` `{valor_quadro}` em TODAS as templates. Teste novo em [templates.test.ts](src/lib/__tests__/templates.test.ts) tranca isto (270€→sinal 81€). **Preflight OK.**
  - **Migração 096:** ✅ **CORRIDA** (Maria, sessão 148). [`096_templates_pre_reserva_indeciso_vaga.sql`](supabase/migrations/096_templates_pre_reserva_indeciso_vaga.sql) reescreveu as 2 templates "tamanho ainda por escolher" (PT+EN): (a) frase "ainda temos vaga para a data" (paridade com as "tamanho escolhido"); (b) valores fixos (300€/90€/120€) → variáveis `{valor_quadro}`/`{valor_sinal}`/`{valor_2a/3a_parcela}`; (c) parágrafo "Once the booking is confirmed…" acrescentado à EN. Corpo por inteiro, idempotente.
  - **Falta o deploy do código** (o fix do desconto em templates.ts): sem ele, os `{valor_*}` nas "indeciso" (agora dinâmicos na BD) ainda calculam pela regra antiga (snapshot ganha ao orçamento). Ir a produção com o próximo push do fbr-admin.
- [x] **Sessão 147 — Migração 095 (templates: frase do link de acompanhamento):** ✅ **CORRIDA** (Maria, sessão 147). Trocou a frase que introduz o `{link_status}` nas 6 templates PT+EN por uma que explica que é acompanhamento vivo de todas as fases + previsão da data de entrega. Só dados, idempotente.
- [ ] **Sessão 145 (Emoldurar Flores Secas — novo serviço, 2 repos, NÃO commitado ainda):**
  - **Passo manual 1 (BD, ANTES de qualquer deploy):** correr [`094_emoldurar_flores_secas.sql`](supabase/migrations/094_emoldurar_flores_secas.sql) no SQL Editor. Cria `orders.service_type/dried_approach/dried_condition/client_photos`, os 3 preços `secas_*` (200/270/360€) e o bucket privado `bouquet-photos`. Verificação no fim do ficheiro.
  - **Passo manual 2 (se o INSERT do bucket falhar):** criar à mão em Dashboard → Storage → New bucket `bouquet-photos` **privado** (o site sobe fotos com service role; sem policies).
  - **Ordem de deploy:** migração → deploy admin (Vercel push) **e** deploy site (develop→main). O admin já **lê** as colunas novas e o site já as **insere**; se o deploy for antes da migração, parte.
  - **Preços:** os 3 valores ficam em Finanças → Catálogo (`base_frame` `secas_*`), afináveis; aumentos futuros não recalculam encomendas antigas (snapshot).
  - **Smoke (site):** abrir `/reservar-emoldurar-flores-secas` (e `/en/book-dried-flower-framing`); submeter um pedido de teste com 1-2 fotos → entra no admin com badge "Secas", secção âmbar com abordagem/estado/fotos (signed URL); CTA "Reservar agora" na página informativa `/emoldurar-flores-secas`.
  - **Smoke (admin):** filtro "Todos / Preservação / Flores secas" na lista (só aparece havendo encomendas secas); ao marcar 1º pagamento numa encomenda secas com Google ligado → pasta "Emoldurar Flores Secas/…/Fotos do cliente" criada no Drive e fotos movidas (Storage esvazia); botão "Remover fotos" no workbench; seta "Voltar" restaura a lista onde estavas (bug corrigido).
  - **Adiado:** criar encomenda secas **manualmente** pelo admin (sheet "Nova encomenda" ainda só cria preservação; intake é o form público); actualizar aba **Ecossistema** + `docs/ECOSYSTEM.md` com o form novo + bucket ([[feedback_novas_plataformas_ecossistema]]).
  - **Lembrete (Maria pediu):** re-adicionar no form de secas o link "ver exemplos de fundos no Instagram" (no campo do fundo do quadro) **quando houver mais publicações lá** — removido para já; há um `TODO` no `EmoldurarForm.jsx` junto ao campo `tipoFundo`.
- [ ] **Sessão 144 (fbr-voucher, EM PRODUÇÃO 16/07):** smoke no telemóvel via WhatsApp: reabrir o link de um vale real → envelope com selo e nome centrados; tentar pinch na página → já não faz zoom do browser (com o cartão aberto, o pinch próprio do cartão continua a funcionar); avisar a Diana/Teresa que podem reabrir o link (o problema era zoom acidental, não os dados)
- [ ] **Sessão 143 (fbr-website, EM PRODUÇÃO desde 14/07 — merge `0c12950`, deploy READY, verificado por script):** smoke visual da Maria em produção: campo do telemóvel formata sozinho ("912 345 678") e avisa algarismos a mais/menos ao sair; +351 a começar por 8 → erro do 1º dígito; email "teste@gmail.con" → sugestão clicável; "maria@gmail.com." → ponto some ao sair; form de reserva com `?vale=UMCODIGO` → código e "como conheceu" preenchidos; código inventado (XXXXXX) → aviso "não encontrámos" (já verificado por script, mas vale ver o aspecto); dropdown com países novos (procurar "Macau"); ecrã de sucesso mostra o contacto escolhido (ao submeter a próxima reserva real, ou uma de teste); dos 2 forms (reserva + vale, PT e EN): nº de telemóvel errado (algarismos a mais/menos) → mensagem clara ao sair do campo e no submit; nº certo → submete normalmente; o campo formata sozinho enquanto se escreve ("912 345 678") e colar "+351 912…" limpa o indicativo; email com gralha (ex.: "teste@gmail.con") → sugestão clicável "Quis dizer…?"; submeter a sério 1 reserva de teste → ecrã de sucesso mostra o contacto escolhido e a encomenda entra no admin com o nº limpo; dropdown de indicativos com os países novos (procurar "Macau"). Depois do OK → commit + push (develop→main como habitual no site).
- [ ] **Sessão 141 (fbr-website, EM PRODUÇÃO):** (a) smoke: página do bouquet (FAQ nova "eternizar" no topo + link do guia em Investimento), Opções (link "Quanto custa..." no bloco verde do CTA), titles dos separadores; (b) **GSC → Inspeção de URLs → pedir reindexação** das 6 páginas alteradas (bouquet PT/EN, opções PT/EN, artigo quanto-tempo-duram PT/EN)
- [x] **Mig 093** corrida no Supabase ✅ (confirmado pela Maria, 12/07 — deploy da sessão 140 desbloqueado)
- [ ] **Smoke sessão 140 (o que sobrou depois dos cortes da 142):** cards colapsados certos nas várias fases (com resumo, clique abre); estado → "Quadro enviado" com CTT → diálogo de tracking e link "Seguir" a abrir a página dos CTT com o objecto; página de um parceiro → bloco Comissões; Dashboard → alerta de comissões por pagar. (Banner "Próxima acção" e checklist já NÃO existem — removidos na 142.)
- [ ] **Smoke sessão 138 (fbr-voucher, 08/07):** abrir um vale real no telemóvel → envelope diz "Para {nome do destinatário}" e caem pétalas douradas ao abrir; no resumo, tocar no código → "Copiado ✓"; botão "Partilhar este vale" abre a partilha do telemóvel; enviar o link a si própria no WhatsApp → o preview mostra a capa do cartão (a Meta pode demorar a renovar previews antigos em cache)
- [ ] **Smoke fbr-voucher (07/07, TUDO já em produção):** abrir voucher.floresabeirario.pt com um código real → cartão carrega (muito mais rápido) e dados certos; código errado (ex.: XXXXXX) → volta à pesquisa com erro simpático (já não mostra "Joana"); botão **PT/EN** no canto superior direito troca a página toda; verificar a nova secção **Integrações** em /healthchecks (2 sites + RPC do voucher a verde)
- [ ] **Smoke sessão 135 (07/07):** no telemóvel, título "FBR Admin" do header de topo agora centrado (estava puxado à esquerda — lados assimétricos com justify-between; passou a centragem absoluta em [layout.tsx](src/app/(admin)/layout.tsx), só mobile, desktop intocado)
- [ ] **Smoke sessão 133 (correcções de segurança já em produção):** login dos 3 perfis OK; aba WhatsApp abre e as imagens carregam (proxy passou a gated para /media|/suggest|/retry — só o /webhook é isento); confirmar que a Ana já NÃO edita Ideias nem Livro de Receitas (só Tarefas/Parcerias/Chat)
- [x] **fbr-website:** correcções M1/M2 EM PRODUÇÃO ✅ (verificado na sessão 139: `1cdc431` é antepassado de `main` — entrou no merge `711ca4b` da 133; este pendente estava obsoleto)
- [ ] **Backup pré-expurgo (sessão 139):** `_privado/backup-pre-expurgo/fbr-admin-completo-2026-07-11.bundle` CONTÉM o histórico antigo com PII — guardar até teres confiança total (umas semanas) e depois **apagar**
- [x] **Mig 091** corrida no Supabase ✅ (confirmado pela Maria, 04/07 — deploy das etiquetas WhatsApp desbloqueado)
- [x] **Mig 089** (lembretes de tarefas) corrida + secret **`CRON_SECRET`** criado no GitHub ✅ (confirmado pela Maria, 05/07)
- [ ] **Smoke sessão 131:** botão "Etiquetas" na aba WhatsApp (mudar cor/nome, criar nova, atribuir a conversa); ✓ cinza em vez de 📱 nas mensagens enviadas
- [ ] **Smoke sessão 130:** sino na sidebar da PWA no telemóvel → "Notificações ligadas" (só funciona em produção)
- [x] **fbr-website:** merge develop→main FEITO na sessão 133 (`711ca4b`) — segurança + sessão 126 (cookies/keywords/UltraVue) + perf em produção

### Próximo passo concreto
Roadmap aprovado (sessão 124, prioridades da Maria — [[project_prioridades_roadmap_124]]):
1. ~~Varrimento `formatDateTimeLisbon`~~ ✅ FEITO na 133 (todos os HH:mm sobre timestamptz; export-csv.ts fica, é server-only)
2. **Item 4 restante:** tipos gerados do Supabase no preflight (precisa do access token da Maria — por isso a 124 usou o schema-drift offline)
3. **Item 8:** vista "Hoje" no Dashboard (experimental, fácil de remover) + relatório mensal interno
4. ~~**2c:** expurgar histórico git~~ ✅ FEITO na 139 (filter-repo + force push; 14 paths, incl. conteúdo das migs 006/014)
5. ~~Motor de cadência de comunicação (104)~~ e ~~absorver fbr-tracking no fbr-website~~ — a Maria disse **NÃO por agora** (11/07/2026); fora do roadmap activo

---

### Fases do projecto
- [x] **Fase 1** — Fundação: Supabase ligado, autenticação, layout/navegação ✅
- [x] **Fase 2** — Preservação de Flores: tabela, workbench, estados, orçamento, permissões ✅
- [x] **Fase 3** — Vale-Presente (admin + site público `voucher.floresabeirario.pt`) + Status ✅
- [x] **Fase 4** — Dashboard + Tarefas + Métricas ✅
- [x] **Fase 5** — Formulários públicos + Parcerias ✅
- [x] **Fase 5.5** — Afinações pós-uso ✅
- [~] **Fase 6** — Integrações + PWA + RGPD completo ← **EM CURSO**

---

## O que está feito (estado actual da plataforma)

- Next.js 16 + shadcn/ui + Supabase ligado, deploy em `admin.floresabeirario.pt`
- Login Netflix com fotos, **email+password** (António admin, MJ admin, Ana viewer); permissões admin/viewer em todas as abas; gate de equipa no proxy (sessão 124); policies centralizadas em `is_team_admin()`/`is_team_member()` (mig 085) + `TEAM` em [roles.ts](src/lib/auth/roles.ts) como fonte única no código
- **Preservação**: 4 vistas (Tabela / Cards / Calendário / Timeline), grupos colapsáveis, drag-and-drop, workbench 3 colunas (refactorizado na 128: orquestrador 436 linhas + 12 componentes em `_components/`), edição inline, alertas 40%/30%/aprovação, vistas/filtros/colunas guardáveis (sessão 95), detecção de clientes repetidos (avisa, nunca bloqueia), dark mode
- **Vale-Presente** admin + site público `voucher.floresabeirario.pt`
- **Status** admin + site público `status.floresabeirario.pt` (12 fases públicas PT/EN, data prevista auto +6m; redesign "Herbário" na 123)
- **Parcerias** completas (4 categorias, mapa Portugal, interações, acções, Nominatim) + Figuras Públicas
- **Dashboard** com afazeres globais em kanban GTD, recolhas/entregas, alertas; tarefas multi-assignee com lembretes data+hora
- **Métricas** + **Finanças** (6 sub-abas: Painel / P&L por encomenda / Catálogo / Despesas / Faturação / Competição; COGS tudo-ou-nada; helpers em [lib/finance.ts](src/lib/finance.ts))
- **Entregas e Recolhas** (agenda + mapa + notas) · **Livro de Receitas** · **Chat interno** (Realtime) · **Ideias** · **Healthchecks** (com monitorização de erros client-side, mig 086) · **Ecossistema**
- **Pesquisa global** Cmd+K em 5 tipos · **PWA** instalável (iOS + Android) com **notificações push internas** (sessão 130: na hora + diárias 7h + lembretes pontuais via GitHub Actions)
- **Integrações Google**: OAuth, pastas Drive auto ao 1º pagamento, Calendar, **Gmail no workbench** (só-leitura, sessão 105)
- **WhatsApp Cloud API** end-to-end (sessões 97-99): webhook, aba `/whatsapp` com avatares/vistos/etiquetas geríveis, media→Drive; registo manual por workbench também existe (sessão 65)
- **Assistente AI "Claude"** (Anthropic API, sessão 119 v2) + **Templates de mensagens** (29 PT+EN, picker com snippets/pesquisa, pares PT/EN na gestão)
- **RGPD**: exportação JSON+PDF, retenção 10 anos com anonimização, audit log UI
- **Backup diário da BD → Drive** (sessão 124: cron 05:00 UTC, 22 tabelas, rotação 14d + mensais + Janeiros; healthcheck próprio)
- **Forms públicos fechados de ponta a ponta** (mig 084 + service role no site; Turnstile server-side) · **CI** GitHub Actions corre `npm run preflight` · anti-drift tipos↔BD no preflight ([lib/schema-drift.ts](src/lib/schema-drift.ts))
- 92 migrações (006/014 são stubs — conteúdo com PII expurgado do histórico na sessão 139); 100 testes vitest; smoke Playwright (`npm run smoke`)

---

## Últimas sessões (detalhe compacto)

### Sessão 152 (2026-08-17) — Converter encomenda para "flores secas" + cor própria do serviço
- **O quê (pedido da Maria):** uma noiva reservou preservação mas o ramo já vinha quase todo seco → não faz sentido cobrar preços de preservação. O select "Tipo de serviço" no rodapé do workbench passa a ter os **3** tipos (secas era selo read-only desde a 149). Trocar de/para secas abre `ServiceChangeDialog` novo que explica antes de aplicar: tabela de preços muda, orçamento recalcula **só se ainda for o automático** (editado à mão fica intocado + dica do botão "Recalcular"), "Flores na prensa" sai/entra da timeline pública e do selector, campos das secas ficam vazios (veio pelo form de preservação) e aviso extra se a encomenda estiver mesmo em "Flores na prensa". Preservação ↔ Recriação continua instantâneo (mesmos preços).
- **Recalculo do orçamento:** `service_type` entra no `needsPrev` e no `pricingFieldChanged` de `updateOrderAction` (só quando muda de valor **e** `budget == pricing_snapshot.total`). `computePricingSnapshot` já escolhia a base `secas_` pelo `service_type` — nada a mudar em pricing.ts.
- **Cor do serviço (2.ª parte do pedido, 2 rondas):** na lista o selo SECAS, "Entrega agendada" e "30% pago" eram todos âmbar. Decisão da Maria: **cores de estado e pagamento NÃO mudam** e **sem bolinhas** → mudou só o serviço "secas". 1.ª tentativa verde-esmeralda em tudo → ela viu que o selo **batia com a pílula verde "Contactada"** na mesma linha. Final: **selo/tag/aba em castanho da marca** (`bg-cocoa-900 text-cream-50` — o único chip escuro da linha, não colide com pastéis) e **cartão do workbench fica verde** (accent/ring/bg/links; lá não há pílulas a competir). "Entrega agendada" e "30% pago" continuam ambos âmbar entre si — limite aceite desta opção.
- **Armadilha para a próxima:** as pílulas da lista já ocupam quase toda a paleta pastel (estados 16 cores, pagamento 4, "Contactada" verde, "Nova" sky, "Recriação" violeta). Para etiquetas NOVAS na lista, preferir chip escuro/neutro em vez de procurar mais um pastel livre.
- **Ficheiros:** [closing-cards.tsx](src/app/(admin)/preservacao/[id]/_components/closing-cards.tsx) (`ServiceChangeDialog` + MetaFooter), [actions.ts](src/app/(admin)/preservacao/actions.ts), [preservacao-client.tsx](src/app/(admin)/preservacao/preservacao-client.tsx), [header.tsx](src/app/(admin)/preservacao/[id]/_components/header.tsx), [dried-flowers-card.tsx](src/app/(admin)/preservacao/[id]/_components/dried-flowers-card.tsx).
- **Migrações:** nenhuma. **Smoke:** preflight ✅ (tsc + 103 testes + build) + eslint limpo nos 5 ficheiros; smoke no browser fica para a Maria (bloco de pendentes).
- **Pendente:** confirmar no browser e, se a conversão for frequente, avaliar oferecer o mesmo select na lista (por agora só no workbench, para não haver conversões acidentais).

### Sessão 150 (2026-08-10) — Pôr live o trabalho não-committado de 5 sessões paralelas
- **O quê:** a Maria tinha 5 sessões Claude que trabalharam em simultâneo, todas terminadas, com código por committar espalhado. Consolidei e pus tudo em produção, com verificação das migrações antes de fazer deploy do que delas depende.
- **fbr-admin (`master` `46c944d`, Vercel READY):** um commit com as DUAS frentes que estavam no working tree — (1) **Recriação** (mig 098) e (2) **status secas esconde "Flores na prensa"** no lado admin (mig 097). tsc + 103 testes + build OK (preflight verde). Ficheiros: database.ts, _styles.ts, preservacao-client.tsx, fields.tsx, closing-cards.tsx, workbench-client.tsx, public-status-sync.test.ts (reaponta p/ 097), migs 097+098.
- **fbr-tracking (`develop`+`main` `854998a`):** commit do lado site do "esconder Flores na prensa" (supabase.js/timeline.js/[id].js). Diff real só 65 linhas (o resto era churn de CRLF — commit limpo confirmado por `git diff --cached`). ff-merge develop→main.
- **Migrações verificadas ANTES do deploy (regra [[feedback_migracoes_supabase_aplicadas]]):** 098 confirmada pela Maria; **097 verificada em produção** — chamei a REST com a anon key e o anon já lê `orders.service_type` (só possível com o GRANT da 097). O `supabase.js` do tracking tem fallback `|| 'preservacao'`, portanto nunca partiria mesmo sem a migração.
- **Nota (não-bloqueante):** 3 deploys de commits anteriores da Maria no fbr-admin (mobile workbench: `40d3fa8`/`9edd6b2`/`7b6ab9e`) ficaram em **ERROR** na Vercel, mas o meu commit por cima buildou READY → produção sã. Provável flutuação transitória do turbopack (preflight local, que inclui build, passa). Vale a pena um olho se voltar a acontecer.
- **Outros repos:** fbr-website e fbr-voucher com working tree limpo e develop==main — nada por pôr live. Templates da sessão 148 já estavam committados+pushed (`9b65700`, live).
- **Smoke (Maria):** os das duas frentes (ver bloco de pendentes acima).

### Sessão 147 (2026-08-09) — Lote de afinações UX/fluxo (Maria) em 3 repos + poke + fix de navegação
- **O quê (fbr-admin):** (1) push "Data de entrega das flores" já não dispara em pré-reserva; (2) nova var `{resumo_encomenda}` nas templates (itens+preços, com quantidade e unitário: "2× Mini 20x25 (2 × 90€ = 180€)"); (3) CTT esconde custo/pago (flores e quadro); (4) `{dados_pagamento}` PT inclui Titular; (5) cabeçalho do WhatsApp fixo (min-h-0 só a lista faz scroll); (6) editar tarefas no telemóvel (botões deixam de ser só-hover); (7) tarefa concluída no workbench: toast Anular + secção "N concluídas"; (8) datas de recolha/entrega em mãos bloqueiam antes do evento; (9) "Entrega agendada" âmbar (distinta do rosa da pré-reserva); (10) **poke** em tarefas (👋 → push interno ao responsável, `pokeTaskAction`); (11) vista Cards colapsa Sem resposta/Pré-reservas/Reservas.
- **Fix de navegação (alargado a pedido da Maria):** vista activa da Preservação (Tabela/Cards/…) e sub-aba+modo das Parcerias passam a PERSISTIR (localStorage via `useSyncExternalStore`) — abrir um detalhe e voltar traz de volta a mesma vista (antes recaía sempre na tabela). Novos campo `activeView` em [preservacao-views.ts](src/lib/preservacao-views.ts) e [parcerias-views.ts](src/lib/parcerias-views.ts) NOVO; colapso das Parcerias recalculado pelo padrão "render anterior" (sem setState-em-effect [[feedback_react_set_state_in_effect]]). Vale-presente não tem alternador de vistas → não se aplica.
- **fbr-tracking + fbr-website:** "Em breve" no status público → "Cerca de 6 meses após recebermos as flores + atualizamos a previsão ao longo do processo" (PT/EN); exemplo da localização do evento no form passa a "Coimbra" + dica pede só a cidade.
- **Migrações:** [095](supabase/migrations/095_templates_link_status_wording.sql) ✅ **CORRIDA** (troca a frase do `{link_status}` nas 6 templates PT+EN por uma que explica acompanhamento vivo de todas as fases + previsão da data de entrega). Só dados, idempotente.
- **Deploy:** fbr-admin `master` (este commit); **fbr-tracking `main` `827f844`** e **fbr-website `main` `cc7440e`** já em produção (pushed nesta sessão).
- **Smoke (Maria):** Cards → abrir encomenda → voltar continua em Cards; Parcerias idem (sub-aba+vista); telemóvel: editar tarefa + campo "🔔 Lembrar-me" ao criar (se não aparecer, print); poke a tarefa com outro responsável → notificação; template de confirmação com frase nova do link; status público de encomenda em pré-reserva → texto novo do ~6 meses; CTT sem campos custo/pago.
- **Pendente:** alertas por email no link de status (email opt-in + botão manual) ficaram registados nas "Próximas frentes" (Maria fá-lo quando tiver tempo).

### Sessão 146 (2026-08-09) — Migração Dualhook: chave outbound `dh_live_` (deadline Meta 12/08)
- **O quê:** email + dashboard do Dualhook avisavam que **a partir de 12/08/2026** os pedidos de **saída** que usam a autorização Meta do Dualhook têm de ir por `https://api.dualhook.com` com a chave `dh_live_` (em `WHATSAPP_ACCESS_TOKEN`), senão a Meta rejeita-os. Webhooks de **entrada** não são afectados (continuam a chegar directamente da Meta). Único outbound da plataforma = **download da multimédia recebida** ([media-fetch.ts](src/lib/whatsapp/media-fetch.ts)); envio não implementado.
- **Ficheiros:** [media-fetch.ts](src/lib/whatsapp/media-fetch.ts) — host `graph.facebook.com`→`api.dualhook.com` (const `WHATSAPP_API_BASE`) + o download dos bytes passa a usar a **rota `/content` do Dualhook** (o `url` temporário devolvido pela Meta aponta para o CDN dela e exigia o token Meta original, que já não temos com a `dh_live_`).
- **Passos manuais (Maria, feitos):** criou a chave no dashboard Dualhook (Connection → Overview → Create key), substituiu `WHATSAPP_ACCESS_TOKEN` na Vercel (Production) pela `dh_live_...CzAM` + redeploy. Guardou o token Meta **antigo** numa nota (rede de segurança). Sem migração de BD.
- **Smoke:** deploy `master` `e8b2000` READY em produção (verificado via Vercel MCP); varredura confirmou **zero** `graph.facebook.com` no código e `WHATSAPP_ACCESS_TOKEN` só usado no media-fetch; **teste real da Maria: enviou foto por WhatsApp → guardada na Drive ✅**. tsc limpo + 100 testes.
- **Fix CI/preflight (mesma sessão):** o CI estava vermelho há semanas (emails "CI: All jobs have failed", 11 annotations) e o preflight local falhava — o **pool paralelo do vitest v4** rebenta a carregar vários ficheiros ao mesmo tempo ("Cannot read properties of undefined (reading 'config')"), no ubuntu do CI e no Windows local. Não era bug do código. Correcção: `fileParallelism: false` em [vitest.config.ts](vitest.config.ts) → 100 testes passam em série (~9-12s).
- **Também triado (não são acção nossa):** email Google Maps "Apple Silicon/iOS SDK" = irrelevante (FBR usa Maps só na web, sem app iOS); GitHub "Lembretes de tarefas cancelled" = concorrência normal (cancela disparos sobrepostos de propósito); **Google Cloud trial a expirar (~14/08):** Drive/Gmail/Calendário são grátis e mantêm-se; só a **Google Maps** (sugestões de morada + mapa Entregas/Recolhas) exige cartão — degrada com elegância (campos passam a texto manual, dados intactos; mapa Portugal das Parcerias é SVG, não é Google). Maria inclina-se a **não pagar**; custo real seria ~0€ mas exige cartão. Link "abrir no Maps" em Entregas/Recolhas é URL grátis (não parte). **A decidir depois de 14/08 se as sugestões pararem.**
- **Pendente:** apagar o token Meta antigo da nota após ~1 semana de confiança; PR **Dependabot #3** (`npm-mensal`) continua vermelha na Vercel — bump de TypeScript 7 incompatível com Next 16 ("does not provide the compiler API"); fechar/ignorar a PR (não é produção). Ecossistema/`SECRETS.md` já mencionam o Dualhook; rever se convém anotar o outbound.

### Sessão 145 (2026-07-26) — Novo serviço "Emoldurar Flores Já Secas" (2 repos)
- **O quê (pedido da Maria):** serviço novo para quem tem flores **já secas** (não é preservação de flores frescas). Decisões dela: (a) variante da Preservação (mesma tabela `orders` + `service_type`), não aba nova; (b) fotos do ramo → Supabase Storage temporário, movidas para o Drive ao 1º pagamento; (c) 5 fotos, 10 MB cada; (d) sem data, tipo de ocasião + noivos obrigatórios, estado das flores reformulado, salvaguarda nos ornamentos/pendentes (flores rígidas podem não moldar).
- **Admin:** mig 094 (`service_type`/`dried_approach`/`dried_condition`/`client_photos` + preços `secas_*` 200/270/360 + bucket `bouquet-photos`); [pricing.ts](src/lib/pricing.ts) escolhe base `secas_` por `service_type`; [drive.ts](src/lib/google/drive.ts) ganhou categoria "Emoldurar Flores Secas" + `ensureDriedOrderFolder`/`uploadToDriveFolder`; [order-drive-trigger.ts](src/lib/google/order-drive-trigger.ts) ao 1º pagamento cria pasta e **move fotos Storage→Drive**; [lib/storage/bouquet-photos.ts](src/lib/storage/bouquet-photos.ts) NOVO (signed URLs, download, delete, `cleanupOrphanBouquetPhotos` no cron 7h: canceladas/arquivadas/por-pagar-90d); workbench com [dried-flowers-card.tsx](src/app/(admin)/preservacao/[id]/_components/dried-flowers-card.tsx) NOVO (fundo âmbar, abordagem/estado/fotos + botão remover); lista com filtro segmentado + badge "Secas".
- **Bug corrigido (à parte):** seta de voltar do workbench ([header.tsx](src/app/(admin)/preservacao/[id]/_components/header.tsx)) era `Link` fixo para `/preservacao` (recarregava a lista no estado inicial) → agora `router.back()` restaura vista/filtros/scroll.
- **Site:** rota `/reservar-emoldurar-flores-secas` (EN `/book-dried-flower-framing`) — page.js + `EmoldurarReservarClient.jsx` + `EmoldurarForm.jsx` (reusa namespace `formReserva` + novo `formEmoldurar`) + `EmoldurarReservar.css` (upload) + rota `/api/emoldurar-flores-secas` (multipart, sobe fotos, insere) + `mapEmoldurarToOrder`; CTA "Reservar agora" na página informativa. Namespaces `formEmoldurar`+`reservarEmoldurar` em pt/en.json (inserção cirúrgica preservou CRLF — **atenção `core.autocrlf=true`: `git diff` mostra churn de EOL falso; usar `git -c core.autocrlf=false diff` para ver as ~95 linhas reais**).
- **Refinamento (mesma sessão, feedback da Maria ao ver o develop):** form de secas com **tema azul próprio** (foto `quadro-flores-vidro-sobre-vidro` + tons azuis via `.emoldurar-theme`) para não confundir com a reserva; removido "localização do evento"; "tipo de flores" passou a usar o texto exacto da reserva (não assume ramo); pergunta "abordagem" reescrita + opções explícitas; "Fotos das suas flores" (não "do ramo"); removidos links Como Funciona/Opções (são da preservação), tamanho linka a página do serviço; **"sem extras" passa a último nos DOIS forms** (detecção por-valor, não por índice — mexeu no `ReservarPreservacaoForm` também); "confirmaremos". Admin: subpasta Drive "Fotos do cliente", label workbench "Fotos das flores", `DRIED_APPROACH_LABELS` explícitos.
- **Migrações + passos manuais:** mig 094 ✅ **CORRIDA** (Maria, 26/07) + bucket `bouquet-photos` ✅ criado (privado, confirmado por screenshot). **TUDO EM PRODUÇÃO (26/07):** admin `master` (`659045f`) + **site `main` `dce49a1`** (merge develop→main aprovado pela Maria). Várias rondas de feedback: tema **verde-sálvia** (distinto da reserva dourada e do vale azul), hero `casamento.webp`, campos verdes, abordagem/fotos/tamanho/notas com textos **próprios** do secas (form de preservação **intacto** — um deslize foi revertido). **Smoke:** admin preflight ✅; site build ✅.
- **Pendente:** **smoke no browser da Maria** (submeter pedido de teste com fotos → entra no admin com secção verde/badge "Secas"; marcar 1º pagamento → pasta Drive criada + fotos movidas do Storage); criar secas manual no admin (adiado); actualizar Ecossistema; re-adicionar link Instagram nos fundos quando houver mais publicações (TODO no `EmoldurarForm.jsx`).

> Sessões 127-144 movidas para o [PROGRESS-ARQUIVO.md](PROGRESS-ARQUIVO.md).

---

## Pendências externas (outros repos)

- **Vale de exemplo no site (fbr-website + fbr-voucher, 09/08 — EM PRODUÇÃO, verificado live):** a Maria quis um link na página do vale-presente para as pessoas verem o cartão digital que recebem. **fbr-voucher (`main` `24781cf`):** código reservado `EXEMPLO`/`EXAMPLE` (aliás EN) tratado em [api/voucher.js](../fbr-voucher/fbr-voucher/api/voucher.js) → devolve dados fictícios de um casal (De: Mariana · Para: Sofia & Miguel · 400€ · validade +2 anos, nunca expira) sem tocar na BD; `index.html` mostra selo "Exemplo" (canto sup. esq.), localiza a mensagem via I18N (`exemplo.msg` PT/EN), e **não anexa** `?vale=EXEMPLO` ao CTA de reserva (evita empurrar código fictício para o form). **fbr-website (`main` `6afe9a2`, merge develop→main via worktree p/ não tocar em trabalho não-committado de outra sessão):** link "Ver um exemplo do vale digital →" no hero de [ValeApresenteClient.jsx](../fbr-website/fbr-website/app/vale-presente/ValeApresenteClient.jsx) (chave `vale.verExemplo` PT/EN), aponta a `voucher.floresabeirario.pt/EXEMPLO` (`?lang=en` em EN). **Verificado live:** `/api/voucher?code=EXEMPLO` devolve `exemplo:true` e a página do vale mostra o link. **2.º sítio (a pedido da Maria):** mesmo link também em **/oferecer-preservacao** (namespace `oferecer.verExemplo` PT/EN), na secção do cartão de [OfereceClient.jsx](../fbr-website/fbr-website/app/oferecer-preservacao/OfereceClient.jsx) — deploy `main` `4ce149b`. **Falta só:** smoke visual da Maria (abrir `/EXEMPLO` PT e EN → cartão do casal com selo, envelope "Para Sofia & Miguel", pétalas; confirmar nomes/valor/mensagem a gosto).
  - ⚠️ **Colisão de sessões paralelas (fbr-website):** o working tree tinha trabalho SEO não-committado de outra sessão Claude; para não lhe tocar, o 1.º link (vale-presente) foi committado com staging por hunk no `pt.json`, e o 2.º link (oferecer) foi feito num **worktree limpo a partir de `origin/develop`** (os JSON no working tree ficaram convertidos p/ LF por essa outra sessão — churn falso; ver [[project_parallel_sessions_worktree]]). No merge develop→main do 2.º link, um `git fetch` apanhou o commit **`8655d70` (SEO: sinónimos buquê/conservar + hub-and-spoke + artigo EN)** que a outra sessão entretanto empurrou para develop, e foi para produção junto com o meu (`4ce149b`). Não fiz force-push ao main (destrutivo); o commit deles é trabalho completo destinado ao main na mesma. **Avisado à Maria.**
- **fbr-website** — sessão 126 (cookies/keywords/UltraVue) já foi live no merge da 133; develop e main sincronizados desde a 137. Continuam em aberto as decisões da auditoria 122: aggregateRating? subtítulo no hero? data nas páginas legais? + vídeo `tracking.mp4` (Maria ainda não tem) + ler `?vale=` no form de reserva (pendente da 138).
- **Relatório mensal de analytics (Clarity)** — ✅ FEITO (sessão 134): compilação mensal + email via Resend ligados ao cron `clarity-snapshot` (fbr-website `82d0f60`, main+develop sincronizados, EM PRODUÇÃO). 1.º email (Julho) chega no início de Agosto. Umami continua manual (API paga) — [[project_website_analytics]].
- **fbr-voucher — análise completa + 2 levas de melhorias, TUDO em produção (07/07/2026):**
  - **Leva 1 (`6a0d4b4`):** **(a)** código errado/expirado mostrava o cartão com os nomes-exemplo "Joana/Guilherme e Alexandra" — agora reencaminha para a pesquisa com erro e código pré-preenchido; **(b)** cartão 3D passou de `voucher_azul.pdf` 12,4 MB + pdf.js do cdnjs para 2 WebP pré-renderizados (`img/`, 0,8 MB, −94%; PDF fica como fonte de design, fora do deploy via `.vercelignore`; regenerar com `scripts/render-pdf.mjs`); **(c)** rate limit também no `/api/share` (helper partilhado `api/_ratelimit.js`); **(d)** menores: 500 sem `err.message`, favicon.svg 393 KB removido, `settings.local.json` destracked, OG tags por regex, README.
  - **Leva 2 (`f57d98a`):** **fontes Jost+Homemade Apple servidas localmente** (woff2 em `fonts/`, RGPD: sem pedidos à Google; CSP sem hosts externos); **Cache-Control** no vercel.json (fonts/favicon imutáveis 1 ano, img/ 7 dias SWR); **versão EN completa** com selector PT/EN no canto (motor i18n mínimo `data-i18n`/`-html`/`-ph`, persiste em localStorage, `?lang=` prioritário, default por navigator.language); **smoke movido para o repo** (`scripts/smoke.mjs` auto-contido com mock, 29 verificações).
  - **Admin (`936001e`):** healthcheck novo — categoria Integrações passa a testar voucher.* + status.* alcançáveis e a RPC `get_voucher_by_code` (o caminho de que o site depende). Corre na página e no cron 7h. Preflight OK; sites live a 200.
  - **Arquitectura (sã):** estático + 2 fns serverless, RPC só devolve vales pagos/não-arquivados, XSS ok, headers fortes. **Smoke:** 29/29 verdes (Playwright+Edge), cartão e páginas PT+EN inspeccionados por screenshot; deploy live confirmado (0 refs cdnjs/googleapis, cache imutável nas fontes).
  - **Leva 3 (sessão 138, 08/07):** 9 melhorias de design/UX (envelope personalizado, pétalas, copiar código, `?vale=` no CTA, aviso de expirado, partilhar, OG image 1200×630, reduced-motion, zoom permitido) — detalhe na secção "Últimas sessões".
  - **Não feito de propósito (espera palavra da Maria):** **Umami no voucher** (analytics anónimos, opcional). O vale expirado passou a mostrar aviso discreto na sessão 138 (continua a abrir — o resto é conversa com a cliente).

---

## Próximas frentes (por ordem — ver "Próximo passo concreto" no topo)

- Varrimento `formatDateTimeLisbon` (129) → tipos gerados Supabase no preflight → vista "Hoje" + relatório mensal → expurgo WhatsApp do git (sessão dedicada) → cadência de comunicação (104)
- **Alertas por email no link de status** (a Maria fá-lo quando tiver tempo, sessão 147): cliente opta por receber avisos no `status.floresabeirario.pt` (email + consentimento RGPD numa tabela `status_subscriptions` nova, com GRANT/RLS); no admin (aba Status) um botão **"Notificar cliente desta atualização"** que **só envia quando a Maria carrega** (mudanças de estado acidentais nunca enviam nada); email PT/EN via Resend. Aprovado o approach "email opt-in + botão manual" (nunca automático — [[feedback_nada_de_envio_automatico]]).
- **Chat interno — media** (upload foto/vídeo/áudio; hoje só texto)
- **Mover Competição** de Finanças para Parcerias (decidir: sub-aba ou aba "Inteligência")
- View SQL `order_pnl` para exports/queries ad-hoc (nice-to-have)
- Outras ideias vivem na aba `/ideias` da plataforma

---

## Armadilhas conhecidas (anti-repetição)

- **timestamptz → sempre `formatDateTimeLisbon`** (nunca `format(…HH:mm)` do date-fns em componentes hidratados — React #418, sessão 129)
- **`useEffect+setState` viola ESLint** — usar "store info from previous renders" ([[feedback_react_set_state_in_effect]])
- **`useSyncExternalStore` snapshot** tem de devolver referência cacheada ou dá React #185 ([[feedback_useSyncExternalStore_pitfall]])
- **`INSERT...RETURNING` precisa de GRANT SELECT** — não só INSERT ([[feedback_supabase_rls_pitfalls]]); tabelas novas precisam de GRANT explícito ([[project_supabase_public_grants_2026]])
- **`CREATE TABLE IF NOT EXISTS` é silencioso** se a tabela existe — usar `ALTER TABLE` em migrações subsequentes
- **Migrações no repo ≠ aplicadas em produção** — é a Maria que as corre no SQL Editor; verificar antes de depender ([[feedback_migracoes_supabase_aplicadas]])
- **Vercel não auto-redeploya** ao mudar env vars — forçar; `NEXT_PUBLIC_*` só entra no build seguinte
- **Nada entra em `public/`** que não seja para servir na app (tudo aí é público no deploy)
- **Sessões paralelas** no mesmo working tree — `git status` antes de commitar ([[project_parallel_sessions_worktree]])
- **base-ui (não Radix):** `PopoverTrigger` sem `asChild` — o Trigger já é `<button>`
- **Smoke test obrigatório** antes de fechar sessões que mexem em páginas críticas ([[feedback_smoke_test_obrigatorio]])
- **Ecos do WhatsApp (mensagens que a Maria envia do telemóvel) chegam com atraso** — vêm por um canal separado da Meta (`smb_message_echoes`), mais lento que as mensagens das clientes (`messages`), e agora com o salto extra do Dualhook. A aba `/whatsapp` É tempo real (Supabase Realtime, INSERT em `whatsapp_messages`), por isso aparecem sozinhas quando chegam. Se uma mensagem enviada do telemóvel demora segundos/1-2min a aparecer, é o eco lento da Meta, **não um bug** — o webhook e a inserção estão OK (confirmado 09/08: 200 + zero erros nos logs). Só investigar se uma mensagem de **cliente** falha, ou se demora muitos minutos / nunca chega.
